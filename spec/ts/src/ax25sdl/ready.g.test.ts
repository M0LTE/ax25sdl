// Code generated by codegen/src/Packet.Sdl.CodeGen from spec-sdl/v2.2-errata/management-data-link/yaml/ready.sdl.yaml.
// DO NOT EDIT. Run `dotnet run --project codegen/src/Packet.Sdl.CodeGen` to regenerate.

import { describe, it, expect } from "vitest";
import { ManagementDataLinkReady } from "./ready.g.js";

describe("ManagementDataLinkReady", () => {
  it("source figure", () => {
    expect(ManagementDataLinkReady.source.figure).toBe("figc5.1");
  });

  it("transitions are present", () => {
    expect(ManagementDataLinkReady.transitions).toHaveLength(2);
  });

  it("t01_mdl_negotiate_request", () => {
    const t = ManagementDataLinkReady.transitions.find((x) => x.id === "t01_mdl_negotiate_request");
    expect(t, "transition t01_mdl_negotiate_request not found").toBeDefined();
    if (!t) return;
    expect(t.on).toBe("MDL_NEGOTIATE_request");
    expect(t.next).toBe("Negotiating");
    expect(t.actions).toHaveLength(3);
    expect(t.actions[0].verb).toBe("RC := 0");
    expect(t.actions[0].kind).toBe("processing");
    expect(t.actions[1].verb).toBe("XID_command");
    expect(t.actions[1].kind).toBe("signal_lower");
    expect(t.actions[2].verb).toBe("Start TM201");
    expect(t.actions[2].kind).toBe("processing");
  });

  it("t02_xid_response_received", () => {
    const t = ManagementDataLinkReady.transitions.find((x) => x.id === "t02_xid_response_received");
    expect(t, "transition t02_xid_response_received not found").toBeDefined();
    if (!t) return;
    expect(t.on).toBe("XID_response_received");
    expect(t.next).toBe("Ready");
    expect(t.actions).toHaveLength(1);
    expect(t.actions[0].verb).toBe("MDL_ERROR_indicate_B");
    expect(t.actions[0].kind).toBe("signal_upper");
  });

});
