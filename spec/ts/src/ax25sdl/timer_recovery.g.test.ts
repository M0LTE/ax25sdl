// Code generated by codegen/src/Packet.Sdl.CodeGen from spec-sdl/v2.2-errata/data-link/yaml/timer_recovery.sdl.yaml.
// DO NOT EDIT. Run `dotnet run --project codegen/src/Packet.Sdl.CodeGen` to regenerate.

import { describe, it, expect } from "vitest";
import { DataLinkTimerRecovery } from "./timer_recovery.g.js";

describe("DataLinkTimerRecovery", () => {
  it("source figure", () => {
    expect(DataLinkTimerRecovery.source.figure).toBe("figc4.5");
  });

  it("transitions are present", () => {
    expect(DataLinkTimerRecovery.transitions).toHaveLength(86);
  });

  it("t01_dl_disconnect_request", () => {
    const t = DataLinkTimerRecovery.transitions.find((x) => x.id === "t01_dl_disconnect_request");
    expect(t, "transition t01_dl_disconnect_request not found").toBeDefined();
    if (!t) return;
    expect(t.on).toBe("DL_DISCONNECT_request");
    expect(t.next).toBe("AwaitingRelease");
    expect(t.actions).toHaveLength(5);
    expect(t.actions[0].verb).toBe("discard_I_frame_queue");
    expect(t.actions[0].kind).toBe("processing");
    expect(t.actions[1].verb).toBe("RC := 0");
    expect(t.actions[1].kind).toBe("processing");
    expect(t.actions[2].verb).toBe("DISC (P = 1)");
    expect(t.actions[2].kind).toBe("signal_lower");
    expect(t.actions[3].verb).toBe("Stop T3");
    expect(t.actions[3].kind).toBe("processing");
    expect(t.actions[4].verb).toBe("Start T1");
    expect(t.actions[4].kind).toBe("processing");
  });

  it("t02_dl_data_request", () => {
    const t = DataLinkTimerRecovery.transitions.find((x) => x.id === "t02_dl_data_request");
    expect(t, "transition t02_dl_data_request not found").toBeDefined();
    if (!t) return;
    expect(t.on).toBe("DL_DATA_request");
    expect(t.next).toBe("TimerRecovery");
    expect(t.actions).toHaveLength(1);
    expect(t.actions[0].verb).toBe("Push on I Frame Queue");
    expect(t.actions[0].kind).toBe("internal_out");
  });

  it("t03_i_frame_pops_off_queue_yes", () => {
    const t = DataLinkTimerRecovery.transitions.find((x) => x.id === "t03_i_frame_pops_off_queue_yes");
    expect(t, "transition t03_i_frame_pops_off_queue_yes not found").toBeDefined();
    if (!t) return;
    expect(t.on).toBe("I_frame_pops_off_queue");
    expect(t.next).toBe("TimerRecovery");
    expect(t.guard).toBe("peer_receiver_busy");
    expect(t.actions).toHaveLength(1);
    expect(t.actions[0].verb).toBe("Push I Frame on I Queue");
    expect(t.actions[0].kind).toBe("internal_out");
  });

  it("t03_i_frame_pops_off_queue_no_yes", () => {
    const t = DataLinkTimerRecovery.transitions.find((x) => x.id === "t03_i_frame_pops_off_queue_no_yes");
    expect(t, "transition t03_i_frame_pops_off_queue_no_yes not found").toBeDefined();
    if (!t) return;
    expect(t.on).toBe("I_frame_pops_off_queue");
    expect(t.next).toBe("TimerRecovery");
    expect(t.guard).toBe("not peer_receiver_busy and vs_eq_va_plus_k");
    expect(t.actions).toHaveLength(1);
    expect(t.actions[0].verb).toBe("Push I Frame on I Queue");
    expect(t.actions[0].kind).toBe("internal_out");
  });

  it("t03_i_frame_pops_off_queue_no_no_yes", () => {
    const t = DataLinkTimerRecovery.transitions.find((x) => x.id === "t03_i_frame_pops_off_queue_no_no_yes");
    expect(t, "transition t03_i_frame_pops_off_queue_no_no_yes not found").toBeDefined();
    if (!t) return;
    expect(t.on).toBe("I_frame_pops_off_queue");
    expect(t.next).toBe("TimerRecovery");
    expect(t.guard).toBe("not peer_receiver_busy and not vs_eq_va_plus_k and T1_running");
    expect(t.actions).toHaveLength(6);
    expect(t.actions[0].verb).toBe("N(s) := V(s)");
    expect(t.actions[0].kind).toBe("processing");
    expect(t.actions[1].verb).toBe("N(R) := V(r)");
    expect(t.actions[1].kind).toBe("processing");
    expect(t.actions[2].verb).toBe("P := 0");
    expect(t.actions[2].kind).toBe("processing");
    expect(t.actions[3].verb).toBe("I Command");
    expect(t.actions[3].kind).toBe("signal_lower");
    expect(t.actions[4].verb).toBe("V(s) := V(s) + 1");
    expect(t.actions[4].kind).toBe("processing");
    expect(t.actions[5].verb).toBe("Clear Acknowledge Pending");
    expect(t.actions[5].kind).toBe("processing");
  });

  it("t03_i_frame_pops_off_queue_no_no_no", () => {
    const t = DataLinkTimerRecovery.transitions.find((x) => x.id === "t03_i_frame_pops_off_queue_no_no_no");
    expect(t, "transition t03_i_frame_pops_off_queue_no_no_no not found").toBeDefined();
    if (!t) return;
    expect(t.on).toBe("I_frame_pops_off_queue");
    expect(t.next).toBe("TimerRecovery");
    expect(t.guard).toBe("not peer_receiver_busy and not vs_eq_va_plus_k and not T1_running");
    expect(t.actions).toHaveLength(8);
    expect(t.actions[0].verb).toBe("N(s) := V(s)");
    expect(t.actions[0].kind).toBe("processing");
    expect(t.actions[1].verb).toBe("N(R) := V(r)");
    expect(t.actions[1].kind).toBe("processing");
    expect(t.actions[2].verb).toBe("P := 0");
    expect(t.actions[2].kind).toBe("processing");
    expect(t.actions[3].verb).toBe("I Command");
    expect(t.actions[3].kind).toBe("signal_lower");
    expect(t.actions[4].verb).toBe("V(s) := V(s) + 1");
    expect(t.actions[4].kind).toBe("processing");
    expect(t.actions[5].verb).toBe("Clear Acknowledge Pending");
    expect(t.actions[5].kind).toBe("processing");
    expect(t.actions[6].verb).toBe("Stop T3");
    expect(t.actions[6].kind).toBe("processing");
    expect(t.actions[7].verb).toBe("Start T1");
    expect(t.actions[7].kind).toBe("processing");
  });

  it("t04_dl_unit_data_request", () => {
    const t = DataLinkTimerRecovery.transitions.find((x) => x.id === "t04_dl_unit_data_request");
    expect(t, "transition t04_dl_unit_data_request not found").toBeDefined();
    if (!t) return;
    expect(t.on).toBe("DL_UNIT_DATA_request");
    expect(t.next).toBe("TimerRecovery");
    expect(t.actions).toHaveLength(1);
    expect(t.actions[0].verb).toBe("UI Command");
    expect(t.actions[0].kind).toBe("signal_lower");
  });

  it("t05_dl_flow_off_request_yes", () => {
    const t = DataLinkTimerRecovery.transitions.find((x) => x.id === "t05_dl_flow_off_request_yes");
    expect(t, "transition t05_dl_flow_off_request_yes not found").toBeDefined();
    if (!t) return;
    expect(t.on).toBe("DL_FLOW_OFF_request");
    expect(t.next).toBe("TimerRecovery");
    expect(t.guard).toBe("own_receiver_busy");
    expect(t.actions).toHaveLength(3);
    expect(t.actions[0].verb).toBe("Set Own Receiver Busy");
    expect(t.actions[0].kind).toBe("processing");
    expect(t.actions[1].verb).toBe("RNR Response (F = 0)");
    expect(t.actions[1].kind).toBe("signal_lower");
    expect(t.actions[2].verb).toBe("Clear Acknowledge Pending");
    expect(t.actions[2].kind).toBe("processing");
  });

  it("t05_dl_flow_off_request_no", () => {
    const t = DataLinkTimerRecovery.transitions.find((x) => x.id === "t05_dl_flow_off_request_no");
    expect(t, "transition t05_dl_flow_off_request_no not found").toBeDefined();
    if (!t) return;
    expect(t.on).toBe("DL_FLOW_OFF_request");
    expect(t.next).toBe("TimerRecovery");
    expect(t.guard).toBe("not own_receiver_busy");
    expect(t.actions).toHaveLength(0);
  });

  it("t06_dl_flow_on_request_yes_no", () => {
    const t = DataLinkTimerRecovery.transitions.find((x) => x.id === "t06_dl_flow_on_request_yes_no");
    expect(t, "transition t06_dl_flow_on_request_yes_no not found").toBeDefined();
    if (!t) return;
    expect(t.on).toBe("DL_FLOW_ON_request");
    expect(t.next).toBe("TimerRecovery");
    expect(t.guard).toBe("own_receiver_busy and not T1_running");
    expect(t.actions).toHaveLength(5);
    expect(t.actions[0].verb).toBe("Clear Own Receiver Busy");
    expect(t.actions[0].kind).toBe("processing");
    expect(t.actions[1].verb).toBe("RR Command (P = 0)");
    expect(t.actions[1].kind).toBe("signal_lower");
    expect(t.actions[2].verb).toBe("Clear Acknowledge Pending");
    expect(t.actions[2].kind).toBe("processing");
    expect(t.actions[3].verb).toBe("Stop T3");
    expect(t.actions[3].kind).toBe("processing");
    expect(t.actions[4].verb).toBe("Start T1");
    expect(t.actions[4].kind).toBe("processing");
  });

  it("t06_dl_flow_on_request_yes_yes", () => {
    const t = DataLinkTimerRecovery.transitions.find((x) => x.id === "t06_dl_flow_on_request_yes_yes");
    expect(t, "transition t06_dl_flow_on_request_yes_yes not found").toBeDefined();
    if (!t) return;
    expect(t.on).toBe("DL_FLOW_ON_request");
    expect(t.next).toBe("TimerRecovery");
    expect(t.guard).toBe("own_receiver_busy and T1_running");
    expect(t.actions).toHaveLength(3);
    expect(t.actions[0].verb).toBe("Clear Own Receiver Busy");
    expect(t.actions[0].kind).toBe("processing");
    expect(t.actions[1].verb).toBe("RR Command (P = 0)");
    expect(t.actions[1].kind).toBe("signal_lower");
    expect(t.actions[2].verb).toBe("Clear Acknowledge Pending");
    expect(t.actions[2].kind).toBe("processing");
  });

  it("t06_dl_flow_on_request_no", () => {
    const t = DataLinkTimerRecovery.transitions.find((x) => x.id === "t06_dl_flow_on_request_no");
    expect(t, "transition t06_dl_flow_on_request_no not found").toBeDefined();
    if (!t) return;
    expect(t.on).toBe("DL_FLOW_ON_request");
    expect(t.next).toBe("TimerRecovery");
    expect(t.guard).toBe("not own_receiver_busy");
    expect(t.actions).toHaveLength(0);
  });

  it("t07_dl_connect_request", () => {
    const t = DataLinkTimerRecovery.transitions.find((x) => x.id === "t07_dl_connect_request");
    expect(t, "transition t07_dl_connect_request not found").toBeDefined();
    if (!t) return;
    expect(t.on).toBe("DL_CONNECT_request");
    expect(t.next).toBe("TimerRecovery");
    expect(t.actions).toHaveLength(3);
    expect(t.actions[0].verb).toBe("discard_I_frame_queue");
    expect(t.actions[0].kind).toBe("processing");
    expect(t.actions[1].verb).toBe("Establish_Data_Link");
    expect(t.actions[1].kind).toBe("subroutine");
    expect(t.actions[2].verb).toBe("Set Layer 3 Initiated");
    expect(t.actions[2].kind).toBe("processing");
  });

  it("t08_control_field_error", () => {
    const t = DataLinkTimerRecovery.transitions.find((x) => x.id === "t08_control_field_error");
    expect(t, "transition t08_control_field_error not found").toBeDefined();
    if (!t) return;
    expect(t.on).toBe("control_field_error");
    expect(t.next).toBe("TimerRecovery");
    expect(t.actions).toHaveLength(4);
    expect(t.actions[0].verb).toBe("DL-ERROR Indication (L)");
    expect(t.actions[0].kind).toBe("signal_upper");
    expect(t.actions[1].verb).toBe("discard_I_frame_queue");
    expect(t.actions[1].kind).toBe("processing");
    expect(t.actions[2].verb).toBe("Establish_Data_Link");
    expect(t.actions[2].kind).toBe("subroutine");
    expect(t.actions[3].verb).toBe("Set Layer 3 Initiated");
    expect(t.actions[3].kind).toBe("processing");
  });

  it("t09_info_not_permitted_in_frame", () => {
    const t = DataLinkTimerRecovery.transitions.find((x) => x.id === "t09_info_not_permitted_in_frame");
    expect(t, "transition t09_info_not_permitted_in_frame not found").toBeDefined();
    if (!t) return;
    expect(t.on).toBe("info_not_permitted_in_frame");
    expect(t.next).toBe("TimerRecovery");
    expect(t.actions).toHaveLength(4);
    expect(t.actions[0].verb).toBe("DL-ERROR Indication (M)");
    expect(t.actions[0].kind).toBe("signal_upper");
    expect(t.actions[1].verb).toBe("discard_I_frame_queue");
    expect(t.actions[1].kind).toBe("processing");
    expect(t.actions[2].verb).toBe("Establish_Data_Link");
    expect(t.actions[2].kind).toBe("subroutine");
    expect(t.actions[3].verb).toBe("Set Layer 3 Initiated");
    expect(t.actions[3].kind).toBe("processing");
  });

  it("t10_u_or_s_frame_length_error", () => {
    const t = DataLinkTimerRecovery.transitions.find((x) => x.id === "t10_u_or_s_frame_length_error");
    expect(t, "transition t10_u_or_s_frame_length_error not found").toBeDefined();
    if (!t) return;
    expect(t.on).toBe("u_or_s_frame_length_error");
    expect(t.next).toBe("TimerRecovery");
    expect(t.actions).toHaveLength(4);
    expect(t.actions[0].verb).toBe("DL-ERROR Indication (N)");
    expect(t.actions[0].kind).toBe("signal_upper");
    expect(t.actions[1].verb).toBe("discard_I_frame_queue");
    expect(t.actions[1].kind).toBe("processing");
    expect(t.actions[2].verb).toBe("Establish_Data_Link");
    expect(t.actions[2].kind).toBe("subroutine");
    expect(t.actions[3].verb).toBe("Set Layer 3 Initiated");
    expect(t.actions[3].kind).toBe("processing");
  });

  it("t11_ua_received", () => {
    const t = DataLinkTimerRecovery.transitions.find((x) => x.id === "t11_ua_received");
    expect(t, "transition t11_ua_received not found").toBeDefined();
    if (!t) return;
    expect(t.on).toBe("UA_received");
    expect(t.next).toBe("AwaitingConnection");
    expect(t.actions).toHaveLength(3);
    expect(t.actions[0].verb).toBe("DL-ERROR Indication (N)");
    expect(t.actions[0].kind).toBe("signal_upper");
    expect(t.actions[1].verb).toBe("Establish_Data_Link");
    expect(t.actions[1].kind).toBe("subroutine");
    expect(t.actions[2].verb).toBe("Clear Layer 3 Initiated");
    expect(t.actions[2].kind).toBe("processing");
  });

  it("t12_dm_received", () => {
    const t = DataLinkTimerRecovery.transitions.find((x) => x.id === "t12_dm_received");
    expect(t, "transition t12_dm_received not found").toBeDefined();
    if (!t) return;
    expect(t.on).toBe("DM_received");
    expect(t.next).toBe("Disconnected");
    expect(t.actions).toHaveLength(5);
    expect(t.actions[0].verb).toBe("DL-ERROR Indication (E)");
    expect(t.actions[0].kind).toBe("signal_upper");
    expect(t.actions[1].verb).toBe("DL-DISCONNECT Indication");
    expect(t.actions[1].kind).toBe("signal_upper");
    expect(t.actions[2].verb).toBe("discard_I_frame_queue");
    expect(t.actions[2].kind).toBe("processing");
    expect(t.actions[3].verb).toBe("Stop T1");
    expect(t.actions[3].kind).toBe("processing");
    expect(t.actions[4].verb).toBe("Stop T3");
    expect(t.actions[4].kind).toBe("processing");
  });

  it("t13_sabm_received_no", () => {
    const t = DataLinkTimerRecovery.transitions.find((x) => x.id === "t13_sabm_received_no");
    expect(t, "transition t13_sabm_received_no not found").toBeDefined();
    if (!t) return;
    expect(t.on).toBe("SABM_received");
    expect(t.next).toBe("Connected");
    expect(t.guard).toBe("not vs_eq_va");
    expect(t.actions).toHaveLength(12);
    expect(t.actions[0].verb).toBe("F := P");
    expect(t.actions[0].kind).toBe("processing");
    expect(t.actions[1].verb).toBe("set_version_2_0");
    expect(t.actions[1].kind).toBe("processing");
    expect(t.actions[2].verb).toBe("UA");
    expect(t.actions[2].kind).toBe("signal_lower");
    expect(t.actions[3].verb).toBe("Clear Exception Conditions");
    expect(t.actions[3].kind).toBe("subroutine");
    expect(t.actions[4].verb).toBe("DL-ERROR Indication (F)");
    expect(t.actions[4].kind).toBe("signal_upper");
    expect(t.actions[5].verb).toBe("discard_I_frame_queue");
    expect(t.actions[5].kind).toBe("processing");
    expect(t.actions[6].verb).toBe("DL-CONNECT Indication");
    expect(t.actions[6].kind).toBe("signal_upper");
    expect(t.actions[7].verb).toBe("Stop T1");
    expect(t.actions[7].kind).toBe("processing");
    expect(t.actions[8].verb).toBe("Stop T3");
    expect(t.actions[8].kind).toBe("processing");
    expect(t.actions[9].verb).toBe("V(s) := 0");
    expect(t.actions[9].kind).toBe("processing");
    expect(t.actions[10].verb).toBe("V(a) := 0");
    expect(t.actions[10].kind).toBe("processing");
    expect(t.actions[11].verb).toBe("V(r) := 0");
    expect(t.actions[11].kind).toBe("processing");
  });

  it("t13_sabm_received_yes", () => {
    const t = DataLinkTimerRecovery.transitions.find((x) => x.id === "t13_sabm_received_yes");
    expect(t, "transition t13_sabm_received_yes not found").toBeDefined();
    if (!t) return;
    expect(t.on).toBe("SABM_received");
    expect(t.next).toBe("Connected");
    expect(t.guard).toBe("vs_eq_va");
    expect(t.actions).toHaveLength(5);
    expect(t.actions[0].verb).toBe("F := P");
    expect(t.actions[0].kind).toBe("processing");
    expect(t.actions[1].verb).toBe("set_version_2_0");
    expect(t.actions[1].kind).toBe("processing");
    expect(t.actions[2].verb).toBe("UA");
    expect(t.actions[2].kind).toBe("signal_lower");
    expect(t.actions[3].verb).toBe("Clear Exception Conditions");
    expect(t.actions[3].kind).toBe("subroutine");
    expect(t.actions[4].verb).toBe("DL-ERROR Indication (F)");
    expect(t.actions[4].kind).toBe("signal_upper");
  });

  it("t14_sabme_received_no", () => {
    const t = DataLinkTimerRecovery.transitions.find((x) => x.id === "t14_sabme_received_no");
    expect(t, "transition t14_sabme_received_no not found").toBeDefined();
    if (!t) return;
    expect(t.on).toBe("SABME_received");
    expect(t.next).toBe("Connected");
    expect(t.guard).toBe("not vs_eq_va");
    expect(t.actions).toHaveLength(12);
    expect(t.actions[0].verb).toBe("F := P");
    expect(t.actions[0].kind).toBe("processing");
    expect(t.actions[1].verb).toBe("Set Version 2.2");
    expect(t.actions[1].kind).toBe("processing");
    expect(t.actions[2].verb).toBe("UA");
    expect(t.actions[2].kind).toBe("signal_lower");
    expect(t.actions[3].verb).toBe("Clear Exception Conditions");
    expect(t.actions[3].kind).toBe("subroutine");
    expect(t.actions[4].verb).toBe("DL-ERROR Indication (F)");
    expect(t.actions[4].kind).toBe("signal_upper");
    expect(t.actions[5].verb).toBe("discard_I_frame_queue");
    expect(t.actions[5].kind).toBe("processing");
    expect(t.actions[6].verb).toBe("DL-CONNECT Indication");
    expect(t.actions[6].kind).toBe("signal_upper");
    expect(t.actions[7].verb).toBe("Stop T1");
    expect(t.actions[7].kind).toBe("processing");
    expect(t.actions[8].verb).toBe("Stop T3");
    expect(t.actions[8].kind).toBe("processing");
    expect(t.actions[9].verb).toBe("V(s) := 0");
    expect(t.actions[9].kind).toBe("processing");
    expect(t.actions[10].verb).toBe("V(a) := 0");
    expect(t.actions[10].kind).toBe("processing");
    expect(t.actions[11].verb).toBe("V(r) := 0");
    expect(t.actions[11].kind).toBe("processing");
  });

  it("t14_sabme_received_yes", () => {
    const t = DataLinkTimerRecovery.transitions.find((x) => x.id === "t14_sabme_received_yes");
    expect(t, "transition t14_sabme_received_yes not found").toBeDefined();
    if (!t) return;
    expect(t.on).toBe("SABME_received");
    expect(t.next).toBe("Connected");
    expect(t.guard).toBe("vs_eq_va");
    expect(t.actions).toHaveLength(5);
    expect(t.actions[0].verb).toBe("F := P");
    expect(t.actions[0].kind).toBe("processing");
    expect(t.actions[1].verb).toBe("Set Version 2.2");
    expect(t.actions[1].kind).toBe("processing");
    expect(t.actions[2].verb).toBe("UA");
    expect(t.actions[2].kind).toBe("signal_lower");
    expect(t.actions[3].verb).toBe("Clear Exception Conditions");
    expect(t.actions[3].kind).toBe("subroutine");
    expect(t.actions[4].verb).toBe("DL-ERROR Indication (F)");
    expect(t.actions[4].kind).toBe("signal_upper");
  });

  it("t15_frmr_received", () => {
    const t = DataLinkTimerRecovery.transitions.find((x) => x.id === "t15_frmr_received");
    expect(t, "transition t15_frmr_received not found").toBeDefined();
    if (!t) return;
    expect(t.on).toBe("FRMR_received");
    expect(t.next).toBe("AwaitingConnection");
    expect(t.actions).toHaveLength(4);
    expect(t.actions[0].verb).toBe("DL-ERROR Indication (K)");
    expect(t.actions[0].kind).toBe("signal_upper");
    expect(t.actions[1].verb).toBe("set_version_2_0");
    expect(t.actions[1].kind).toBe("processing");
    expect(t.actions[2].verb).toBe("Establish_Data_Link");
    expect(t.actions[2].kind).toBe("subroutine");
    expect(t.actions[3].verb).toBe("Clear Layer 3 Initiated");
    expect(t.actions[3].kind).toBe("processing");
  });

  it("t16_ui_received_yes", () => {
    const t = DataLinkTimerRecovery.transitions.find((x) => x.id === "t16_ui_received_yes");
    expect(t, "transition t16_ui_received_yes not found").toBeDefined();
    if (!t) return;
    expect(t.on).toBe("UI_received");
    expect(t.next).toBe("TimerRecovery");
    expect(t.guard).toBe("P_eq_1");
    expect(t.actions).toHaveLength(2);
    expect(t.actions[0].verb).toBe("UI Check");
    expect(t.actions[0].kind).toBe("subroutine");
    expect(t.actions[1].verb).toBe("Enquiry_Response_F_1");
    expect(t.actions[1].kind).toBe("subroutine");
  });

  it("t16_ui_received_no", () => {
    const t = DataLinkTimerRecovery.transitions.find((x) => x.id === "t16_ui_received_no");
    expect(t, "transition t16_ui_received_no not found").toBeDefined();
    if (!t) return;
    expect(t.on).toBe("UI_received");
    expect(t.next).toBe("TimerRecovery");
    expect(t.guard).toBe("not P_eq_1");
    expect(t.actions).toHaveLength(1);
    expect(t.actions[0].verb).toBe("UI Check");
    expect(t.actions[0].kind).toBe("subroutine");
  });

  it("t17_disc_received", () => {
    const t = DataLinkTimerRecovery.transitions.find((x) => x.id === "t17_disc_received");
    expect(t, "transition t17_disc_received not found").toBeDefined();
    if (!t) return;
    expect(t.on).toBe("DISC_received");
    expect(t.next).toBe("Disconnected");
    expect(t.actions).toHaveLength(6);
    expect(t.actions[0].verb).toBe("discard_I_frame_queue");
    expect(t.actions[0].kind).toBe("processing");
    expect(t.actions[1].verb).toBe("F := P");
    expect(t.actions[1].kind).toBe("processing");
    expect(t.actions[2].verb).toBe("UA");
    expect(t.actions[2].kind).toBe("signal_lower");
    expect(t.actions[3].verb).toBe("DL-DISCONNECT Indication");
    expect(t.actions[3].kind).toBe("signal_upper");
    expect(t.actions[4].verb).toBe("Stop T1");
    expect(t.actions[4].kind).toBe("processing");
    expect(t.actions[5].verb).toBe("Stop T3");
    expect(t.actions[5].kind).toBe("processing");
  });

  it("t18_rr_received_no_no", () => {
    const t = DataLinkTimerRecovery.transitions.find((x) => x.id === "t18_rr_received_no_no");
    expect(t, "transition t18_rr_received_no_no not found").toBeDefined();
    if (!t) return;
    expect(t.on).toBe("RR_received");
    expect(t.next).toBe("Undefined");
    expect(t.guard).toBe("not response_and_F_eq_1 and not command_and_P_eq_1");
    expect(t.actions).toHaveLength(1);
    expect(t.actions[0].verb).toBe("clear_peer_receiver_busy");
    expect(t.actions[0].kind).toBe("processing");
  });

  it("t18_rr_received_no_yes_yes", () => {
    const t = DataLinkTimerRecovery.transitions.find((x) => x.id === "t18_rr_received_no_yes_yes");
    expect(t, "transition t18_rr_received_no_yes_yes not found").toBeDefined();
    if (!t) return;
    expect(t.on).toBe("RR_received");
    expect(t.next).toBe("TimerRecovery");
    expect(t.guard).toBe("not response_and_F_eq_1 and command_and_P_eq_1 and va_le_nr_le_vs");
    expect(t.actions).toHaveLength(3);
    expect(t.actions[0].verb).toBe("clear_peer_receiver_busy");
    expect(t.actions[0].kind).toBe("processing");
    expect(t.actions[1].verb).toBe("Enquiry_Response_F_1");
    expect(t.actions[1].kind).toBe("subroutine");
    expect(t.actions[2].verb).toBe("V(a) := N(r)");
    expect(t.actions[2].kind).toBe("processing");
  });

  it("t18_rr_received_no_yes_no_no", () => {
    const t = DataLinkTimerRecovery.transitions.find((x) => x.id === "t18_rr_received_no_yes_no_no");
    expect(t, "transition t18_rr_received_no_yes_no_no not found").toBeDefined();
    if (!t) return;
    expect(t.on).toBe("RR_received");
    expect(t.next).toBe("AwaitingConnection");
    expect(t.guard).toBe("not response_and_F_eq_1 and command_and_P_eq_1 and not va_le_nr_le_vs and not version_2_2");
    expect(t.actions).toHaveLength(3);
    expect(t.actions[0].verb).toBe("clear_peer_receiver_busy");
    expect(t.actions[0].kind).toBe("processing");
    expect(t.actions[1].verb).toBe("Enquiry_Response_F_1");
    expect(t.actions[1].kind).toBe("subroutine");
    expect(t.actions[2].verb).toBe("N(r) Error Recovery");
    expect(t.actions[2].kind).toBe("subroutine");
  });

  it("t18_rr_received_no_yes_no_yes", () => {
    const t = DataLinkTimerRecovery.transitions.find((x) => x.id === "t18_rr_received_no_yes_no_yes");
    expect(t, "transition t18_rr_received_no_yes_no_yes not found").toBeDefined();
    if (!t) return;
    expect(t.on).toBe("RR_received");
    expect(t.next).toBe("AwaitingV22Connection");
    expect(t.guard).toBe("not response_and_F_eq_1 and command_and_P_eq_1 and not va_le_nr_le_vs and version_2_2");
    expect(t.actions).toHaveLength(3);
    expect(t.actions[0].verb).toBe("clear_peer_receiver_busy");
    expect(t.actions[0].kind).toBe("processing");
    expect(t.actions[1].verb).toBe("Enquiry_Response_F_1");
    expect(t.actions[1].kind).toBe("subroutine");
    expect(t.actions[2].verb).toBe("N(r) Error Recovery");
    expect(t.actions[2].kind).toBe("subroutine");
  });

  it("t18_rr_received_yes_no_no", () => {
    const t = DataLinkTimerRecovery.transitions.find((x) => x.id === "t18_rr_received_yes_no_no");
    expect(t, "transition t18_rr_received_yes_no_no not found").toBeDefined();
    if (!t) return;
    expect(t.on).toBe("RR_received");
    expect(t.next).toBe("AwaitingConnection");
    expect(t.guard).toBe("response_and_F_eq_1 and not va_le_nr_le_vs and not version_2_2");
    expect(t.actions).toHaveLength(4);
    expect(t.actions[0].verb).toBe("clear_peer_receiver_busy");
    expect(t.actions[0].kind).toBe("processing");
    expect(t.actions[1].verb).toBe("Stop T1");
    expect(t.actions[1].kind).toBe("processing");
    expect(t.actions[2].verb).toBe("Select_T1_Value");
    expect(t.actions[2].kind).toBe("subroutine");
    expect(t.actions[3].verb).toBe("N(r) Error Recovery");
    expect(t.actions[3].kind).toBe("subroutine");
  });

  it("t18_rr_received_yes_no_yes", () => {
    const t = DataLinkTimerRecovery.transitions.find((x) => x.id === "t18_rr_received_yes_no_yes");
    expect(t, "transition t18_rr_received_yes_no_yes not found").toBeDefined();
    if (!t) return;
    expect(t.on).toBe("RR_received");
    expect(t.next).toBe("AwaitingV22Connection");
    expect(t.guard).toBe("response_and_F_eq_1 and not va_le_nr_le_vs and version_2_2");
    expect(t.actions).toHaveLength(4);
    expect(t.actions[0].verb).toBe("clear_peer_receiver_busy");
    expect(t.actions[0].kind).toBe("processing");
    expect(t.actions[1].verb).toBe("Stop T1");
    expect(t.actions[1].kind).toBe("processing");
    expect(t.actions[2].verb).toBe("Select_T1_Value");
    expect(t.actions[2].kind).toBe("subroutine");
    expect(t.actions[3].verb).toBe("N(r) Error Recovery");
    expect(t.actions[3].kind).toBe("subroutine");
  });

  it("t18_rr_received_yes_yes_no", () => {
    const t = DataLinkTimerRecovery.transitions.find((x) => x.id === "t18_rr_received_yes_yes_no");
    expect(t, "transition t18_rr_received_yes_yes_no not found").toBeDefined();
    if (!t) return;
    expect(t.on).toBe("RR_received");
    expect(t.next).toBe("TimerRecovery");
    expect(t.guard).toBe("response_and_F_eq_1 and va_le_nr_le_vs and not vs_eq_va");
    expect(t.actions).toHaveLength(8);
    expect(t.actions[0].verb).toBe("clear_peer_receiver_busy");
    expect(t.actions[0].kind).toBe("processing");
    expect(t.actions[1].verb).toBe("Stop T1");
    expect(t.actions[1].kind).toBe("processing");
    expect(t.actions[2].verb).toBe("Select_T1_Value");
    expect(t.actions[2].kind).toBe("subroutine");
    expect(t.actions[3].verb).toBe("V(a) := N(r)");
    expect(t.actions[3].kind).toBe("processing");
    expect(t.actions[4].verb).toBe("Invoke Retransmission");
    expect(t.actions[4].kind).toBe("subroutine");
    expect(t.actions[5].verb).toBe("Stop T3");
    expect(t.actions[5].kind).toBe("processing");
    expect(t.actions[6].verb).toBe("Start T1");
    expect(t.actions[6].kind).toBe("processing");
    expect(t.actions[7].verb).toBe("Set Acknowledge Pending");
    expect(t.actions[7].kind).toBe("processing");
  });

  it("t18_rr_received_yes_yes_yes", () => {
    const t = DataLinkTimerRecovery.transitions.find((x) => x.id === "t18_rr_received_yes_yes_yes");
    expect(t, "transition t18_rr_received_yes_yes_yes not found").toBeDefined();
    if (!t) return;
    expect(t.on).toBe("RR_received");
    expect(t.next).toBe("Connected");
    expect(t.guard).toBe("response_and_F_eq_1 and va_le_nr_le_vs and vs_eq_va");
    expect(t.actions).toHaveLength(6);
    expect(t.actions[0].verb).toBe("clear_peer_receiver_busy");
    expect(t.actions[0].kind).toBe("processing");
    expect(t.actions[1].verb).toBe("Stop T1");
    expect(t.actions[1].kind).toBe("processing");
    expect(t.actions[2].verb).toBe("Select_T1_Value");
    expect(t.actions[2].kind).toBe("subroutine");
    expect(t.actions[3].verb).toBe("V(a) := N(r)");
    expect(t.actions[3].kind).toBe("processing");
    expect(t.actions[4].verb).toBe("Start T3");
    expect(t.actions[4].kind).toBe("processing");
    expect(t.actions[5].verb).toBe("RC := 0");
    expect(t.actions[5].kind).toBe("processing");
  });

  it("t19_rnr_received_no_no", () => {
    const t = DataLinkTimerRecovery.transitions.find((x) => x.id === "t19_rnr_received_no_no");
    expect(t, "transition t19_rnr_received_no_no not found").toBeDefined();
    if (!t) return;
    expect(t.on).toBe("RNR_received");
    expect(t.next).toBe("Undefined");
    expect(t.guard).toBe("not response_and_F_eq_1 and not command_and_P_eq_1");
    expect(t.actions).toHaveLength(1);
    expect(t.actions[0].verb).toBe("set_peer_receiver_busy");
    expect(t.actions[0].kind).toBe("processing");
  });

  it("t19_rnr_received_no_yes_yes", () => {
    const t = DataLinkTimerRecovery.transitions.find((x) => x.id === "t19_rnr_received_no_yes_yes");
    expect(t, "transition t19_rnr_received_no_yes_yes not found").toBeDefined();
    if (!t) return;
    expect(t.on).toBe("RNR_received");
    expect(t.next).toBe("TimerRecovery");
    expect(t.guard).toBe("not response_and_F_eq_1 and command_and_P_eq_1 and va_le_nr_le_vs");
    expect(t.actions).toHaveLength(3);
    expect(t.actions[0].verb).toBe("set_peer_receiver_busy");
    expect(t.actions[0].kind).toBe("processing");
    expect(t.actions[1].verb).toBe("Enquiry_Response_F_1");
    expect(t.actions[1].kind).toBe("subroutine");
    expect(t.actions[2].verb).toBe("V(a) := N(r)");
    expect(t.actions[2].kind).toBe("processing");
  });

  it("t19_rnr_received_no_yes_no_no", () => {
    const t = DataLinkTimerRecovery.transitions.find((x) => x.id === "t19_rnr_received_no_yes_no_no");
    expect(t, "transition t19_rnr_received_no_yes_no_no not found").toBeDefined();
    if (!t) return;
    expect(t.on).toBe("RNR_received");
    expect(t.next).toBe("AwaitingConnection");
    expect(t.guard).toBe("not response_and_F_eq_1 and command_and_P_eq_1 and not va_le_nr_le_vs and not version_2_2");
    expect(t.actions).toHaveLength(3);
    expect(t.actions[0].verb).toBe("set_peer_receiver_busy");
    expect(t.actions[0].kind).toBe("processing");
    expect(t.actions[1].verb).toBe("Enquiry_Response_F_1");
    expect(t.actions[1].kind).toBe("subroutine");
    expect(t.actions[2].verb).toBe("N(r) Error Recovery");
    expect(t.actions[2].kind).toBe("subroutine");
  });

  it("t19_rnr_received_no_yes_no_yes", () => {
    const t = DataLinkTimerRecovery.transitions.find((x) => x.id === "t19_rnr_received_no_yes_no_yes");
    expect(t, "transition t19_rnr_received_no_yes_no_yes not found").toBeDefined();
    if (!t) return;
    expect(t.on).toBe("RNR_received");
    expect(t.next).toBe("AwaitingV22Connection");
    expect(t.guard).toBe("not response_and_F_eq_1 and command_and_P_eq_1 and not va_le_nr_le_vs and version_2_2");
    expect(t.actions).toHaveLength(3);
    expect(t.actions[0].verb).toBe("set_peer_receiver_busy");
    expect(t.actions[0].kind).toBe("processing");
    expect(t.actions[1].verb).toBe("Enquiry_Response_F_1");
    expect(t.actions[1].kind).toBe("subroutine");
    expect(t.actions[2].verb).toBe("N(r) Error Recovery");
    expect(t.actions[2].kind).toBe("subroutine");
  });

  it("t19_rnr_received_yes_no_no", () => {
    const t = DataLinkTimerRecovery.transitions.find((x) => x.id === "t19_rnr_received_yes_no_no");
    expect(t, "transition t19_rnr_received_yes_no_no not found").toBeDefined();
    if (!t) return;
    expect(t.on).toBe("RNR_received");
    expect(t.next).toBe("AwaitingConnection");
    expect(t.guard).toBe("response_and_F_eq_1 and not va_le_nr_le_vs and not version_2_2");
    expect(t.actions).toHaveLength(4);
    expect(t.actions[0].verb).toBe("set_peer_receiver_busy");
    expect(t.actions[0].kind).toBe("processing");
    expect(t.actions[1].verb).toBe("Stop T1");
    expect(t.actions[1].kind).toBe("processing");
    expect(t.actions[2].verb).toBe("Select_T1_Value");
    expect(t.actions[2].kind).toBe("subroutine");
    expect(t.actions[3].verb).toBe("N(r) Error Recovery");
    expect(t.actions[3].kind).toBe("subroutine");
  });

  it("t19_rnr_received_yes_no_yes", () => {
    const t = DataLinkTimerRecovery.transitions.find((x) => x.id === "t19_rnr_received_yes_no_yes");
    expect(t, "transition t19_rnr_received_yes_no_yes not found").toBeDefined();
    if (!t) return;
    expect(t.on).toBe("RNR_received");
    expect(t.next).toBe("AwaitingV22Connection");
    expect(t.guard).toBe("response_and_F_eq_1 and not va_le_nr_le_vs and version_2_2");
    expect(t.actions).toHaveLength(4);
    expect(t.actions[0].verb).toBe("set_peer_receiver_busy");
    expect(t.actions[0].kind).toBe("processing");
    expect(t.actions[1].verb).toBe("Stop T1");
    expect(t.actions[1].kind).toBe("processing");
    expect(t.actions[2].verb).toBe("Select_T1_Value");
    expect(t.actions[2].kind).toBe("subroutine");
    expect(t.actions[3].verb).toBe("N(r) Error Recovery");
    expect(t.actions[3].kind).toBe("subroutine");
  });

  it("t19_rnr_received_yes_yes_no", () => {
    const t = DataLinkTimerRecovery.transitions.find((x) => x.id === "t19_rnr_received_yes_yes_no");
    expect(t, "transition t19_rnr_received_yes_yes_no not found").toBeDefined();
    if (!t) return;
    expect(t.on).toBe("RNR_received");
    expect(t.next).toBe("TimerRecovery");
    expect(t.guard).toBe("response_and_F_eq_1 and va_le_nr_le_vs and not vs_eq_va");
    expect(t.actions).toHaveLength(8);
    expect(t.actions[0].verb).toBe("set_peer_receiver_busy");
    expect(t.actions[0].kind).toBe("processing");
    expect(t.actions[1].verb).toBe("Stop T1");
    expect(t.actions[1].kind).toBe("processing");
    expect(t.actions[2].verb).toBe("Select_T1_Value");
    expect(t.actions[2].kind).toBe("subroutine");
    expect(t.actions[3].verb).toBe("V(a) := N(r)");
    expect(t.actions[3].kind).toBe("processing");
    expect(t.actions[4].verb).toBe("Invoke Retransmission");
    expect(t.actions[4].kind).toBe("subroutine");
    expect(t.actions[5].verb).toBe("Stop T3");
    expect(t.actions[5].kind).toBe("processing");
    expect(t.actions[6].verb).toBe("Start T1");
    expect(t.actions[6].kind).toBe("processing");
    expect(t.actions[7].verb).toBe("Set Acknowledge Pending");
    expect(t.actions[7].kind).toBe("processing");
  });

  it("t19_rnr_received_yes_yes_yes", () => {
    const t = DataLinkTimerRecovery.transitions.find((x) => x.id === "t19_rnr_received_yes_yes_yes");
    expect(t, "transition t19_rnr_received_yes_yes_yes not found").toBeDefined();
    if (!t) return;
    expect(t.on).toBe("RNR_received");
    expect(t.next).toBe("Connected");
    expect(t.guard).toBe("response_and_F_eq_1 and va_le_nr_le_vs and vs_eq_va");
    expect(t.actions).toHaveLength(6);
    expect(t.actions[0].verb).toBe("set_peer_receiver_busy");
    expect(t.actions[0].kind).toBe("processing");
    expect(t.actions[1].verb).toBe("Stop T1");
    expect(t.actions[1].kind).toBe("processing");
    expect(t.actions[2].verb).toBe("Select_T1_Value");
    expect(t.actions[2].kind).toBe("subroutine");
    expect(t.actions[3].verb).toBe("V(a) := N(r)");
    expect(t.actions[3].kind).toBe("processing");
    expect(t.actions[4].verb).toBe("Start T3");
    expect(t.actions[4].kind).toBe("processing");
    expect(t.actions[5].verb).toBe("RC := 0");
    expect(t.actions[5].kind).toBe("processing");
  });

  it("t20_lm_seize_confirm_yes", () => {
    const t = DataLinkTimerRecovery.transitions.find((x) => x.id === "t20_lm_seize_confirm_yes");
    expect(t, "transition t20_lm_seize_confirm_yes not found").toBeDefined();
    if (!t) return;
    expect(t.on).toBe("LM_SEIZE_confirm");
    expect(t.next).toBe("TimerRecovery");
    expect(t.guard).toBe("ACK_pending");
    expect(t.actions).toHaveLength(3);
    expect(t.actions[0].verb).toBe("Clear Acknowledge Pending");
    expect(t.actions[0].kind).toBe("processing");
    expect(t.actions[1].verb).toBe("Enquiry Response (F = 0)");
    expect(t.actions[1].kind).toBe("subroutine");
    expect(t.actions[2].verb).toBe("LM_RELEASE Request");
    expect(t.actions[2].kind).toBe("signal_lower");
  });

  it("t20_lm_seize_confirm_no", () => {
    const t = DataLinkTimerRecovery.transitions.find((x) => x.id === "t20_lm_seize_confirm_no");
    expect(t, "transition t20_lm_seize_confirm_no not found").toBeDefined();
    if (!t) return;
    expect(t.on).toBe("LM_SEIZE_confirm");
    expect(t.next).toBe("TimerRecovery");
    expect(t.guard).toBe("not ACK_pending");
    expect(t.actions).toHaveLength(1);
    expect(t.actions[0].verb).toBe("LM_RELEASE Request");
    expect(t.actions[0].kind).toBe("signal_lower");
  });

  it("t21_t1_expiry_no", () => {
    const t = DataLinkTimerRecovery.transitions.find((x) => x.id === "t21_t1_expiry_no");
    expect(t, "transition t21_t1_expiry_no not found").toBeDefined();
    if (!t) return;
    expect(t.on).toBe("T1_expiry");
    expect(t.next).toBe("TimerRecovery");
    expect(t.guard).toBe("not RC_eq_N2");
    expect(t.actions).toHaveLength(2);
    expect(t.actions[0].verb).toBe("Transmit Enquiry");
    expect(t.actions[0].kind).toBe("subroutine");
    expect(t.actions[1].verb).toBe("RC := RC + 1");
    expect(t.actions[1].kind).toBe("processing");
  });

  it("t21_t1_expiry_yes_yes_yes", () => {
    const t = DataLinkTimerRecovery.transitions.find((x) => x.id === "t21_t1_expiry_yes_yes_yes");
    expect(t, "transition t21_t1_expiry_yes_yes_yes not found").toBeDefined();
    if (!t) return;
    expect(t.on).toBe("T1_expiry");
    expect(t.next).toBe("Disconnected");
    expect(t.guard).toBe("RC_eq_N2 and vs_eq_va and peer_busy");
    expect(t.actions).toHaveLength(4);
    expect(t.actions[0].verb).toBe("DL-ERROR Indication (U)");
    expect(t.actions[0].kind).toBe("signal_upper");
    expect(t.actions[1].verb).toBe("DL-DISCONNECT Indication");
    expect(t.actions[1].kind).toBe("signal_upper");
    expect(t.actions[2].verb).toBe("discard_I_frame_queue");
    expect(t.actions[2].kind).toBe("processing");
    expect(t.actions[3].verb).toBe("DM Response (F = 0)");
    expect(t.actions[3].kind).toBe("signal_lower");
  });

  it("t21_t1_expiry_yes_yes_no", () => {
    const t = DataLinkTimerRecovery.transitions.find((x) => x.id === "t21_t1_expiry_yes_yes_no");
    expect(t, "transition t21_t1_expiry_yes_yes_no not found").toBeDefined();
    if (!t) return;
    expect(t.on).toBe("T1_expiry");
    expect(t.next).toBe("Disconnected");
    expect(t.guard).toBe("RC_eq_N2 and vs_eq_va and not peer_busy");
    expect(t.actions).toHaveLength(4);
    expect(t.actions[0].verb).toBe("DL-ERROR Indication (T)");
    expect(t.actions[0].kind).toBe("signal_upper");
    expect(t.actions[1].verb).toBe("DL-DISCONNECT Indication");
    expect(t.actions[1].kind).toBe("signal_upper");
    expect(t.actions[2].verb).toBe("discard_I_frame_queue");
    expect(t.actions[2].kind).toBe("processing");
    expect(t.actions[3].verb).toBe("DM Response (F = 0)");
    expect(t.actions[3].kind).toBe("signal_lower");
  });

  it("t21_t1_expiry_yes_no", () => {
    const t = DataLinkTimerRecovery.transitions.find((x) => x.id === "t21_t1_expiry_yes_no");
    expect(t, "transition t21_t1_expiry_yes_no not found").toBeDefined();
    if (!t) return;
    expect(t.on).toBe("T1_expiry");
    expect(t.next).toBe("Disconnected");
    expect(t.guard).toBe("RC_eq_N2 and not vs_eq_va");
    expect(t.actions).toHaveLength(4);
    expect(t.actions[0].verb).toBe("DL-ERROR Indication (I)");
    expect(t.actions[0].kind).toBe("signal_upper");
    expect(t.actions[1].verb).toBe("DL-DISCONNECT Indication");
    expect(t.actions[1].kind).toBe("signal_upper");
    expect(t.actions[2].verb).toBe("discard_I_frame_queue");
    expect(t.actions[2].kind).toBe("processing");
    expect(t.actions[3].verb).toBe("DM Response (F = 0)");
    expect(t.actions[3].kind).toBe("signal_lower");
  });

  it("t22_i_received_undefined", () => {
    const t = DataLinkTimerRecovery.transitions.find((x) => x.id === "t22_i_received_undefined");
    expect(t, "transition t22_i_received_undefined not found").toBeDefined();
    if (!t) return;
    expect(t.on).toBe("I_received");
    expect(t.next).toBe("Connected");
    expect(t.actions).toHaveLength(2);
    expect(t.actions[0].verb).toBe("DL-ERROR Indication (O)");
    expect(t.actions[0].kind).toBe("signal_upper");
    expect(t.actions[1].verb).toBe("Discard I Frame");
    expect(t.actions[1].kind).toBe("processing");
  });

  it("t22_i_received_undefined_no", () => {
    const t = DataLinkTimerRecovery.transitions.find((x) => x.id === "t22_i_received_undefined_no");
    expect(t, "transition t22_i_received_undefined_no not found").toBeDefined();
    if (!t) return;
    expect(t.on).toBe("I_received");
    expect(t.next).toBe("AwaitingConnection");
    expect(t.guard).toBe("not info_field_length_le_N1_and_content_is_octet_aligned");
    expect(t.actions).toHaveLength(3);
    expect(t.actions[0].verb).toBe("DL-ERROR Indication (O)");
    expect(t.actions[0].kind).toBe("signal_upper");
    expect(t.actions[1].verb).toBe("Establish_Data_Link");
    expect(t.actions[1].kind).toBe("subroutine");
    expect(t.actions[2].verb).toBe("Clear Layer 3 Initiated");
    expect(t.actions[2].kind).toBe("processing");
  });

  it("t22_i_received_undefined_yes_no", () => {
    const t = DataLinkTimerRecovery.transitions.find((x) => x.id === "t22_i_received_undefined_yes_no");
    expect(t, "transition t22_i_received_undefined_yes_no not found").toBeDefined();
    if (!t) return;
    expect(t.on).toBe("I_received");
    expect(t.next).toBe("AwaitingConnection");
    expect(t.guard).toBe("info_field_length_le_N1_and_content_is_octet_aligned and not va_le_nr_le_vs");
    expect(t.actions).toHaveLength(1);
    expect(t.actions[0].verb).toBe("N(r) Recovery");
    expect(t.actions[0].kind).toBe("subroutine");
  });

  it("t22_i_received_undefined_yes_yes_yes_yes", () => {
    const t = DataLinkTimerRecovery.transitions.find((x) => x.id === "t22_i_received_undefined_yes_yes_yes_yes");
    expect(t, "transition t22_i_received_undefined_yes_yes_yes_yes not found").toBeDefined();
    if (!t) return;
    expect(t.on).toBe("I_received");
    expect(t.next).toBe("TimerRecovery");
    expect(t.guard).toBe("info_field_length_le_N1_and_content_is_octet_aligned and va_le_nr_le_vs and own_receive_busy and P_eq_1");
    expect(t.actions).toHaveLength(6);
    expect(t.actions[0].verb).toBe("Check_I_Frame_Acknowledged");
    expect(t.actions[0].kind).toBe("subroutine");
    expect(t.actions[1].verb).toBe("Discard Contents of I Frame");
    expect(t.actions[1].kind).toBe("processing");
    expect(t.actions[2].verb).toBe("F := 1");
    expect(t.actions[2].kind).toBe("processing");
    expect(t.actions[3].verb).toBe("N(r) := V(r)");
    expect(t.actions[3].kind).toBe("processing");
    expect(t.actions[4].verb).toBe("RNR");
    expect(t.actions[4].kind).toBe("signal_lower");
    expect(t.actions[5].verb).toBe("Clear Acknowledge Pending");
    expect(t.actions[5].kind).toBe("processing");
  });

  it("t22_i_received_undefined_yes_yes_yes_no", () => {
    const t = DataLinkTimerRecovery.transitions.find((x) => x.id === "t22_i_received_undefined_yes_yes_yes_no");
    expect(t, "transition t22_i_received_undefined_yes_yes_yes_no not found").toBeDefined();
    if (!t) return;
    expect(t.on).toBe("I_received");
    expect(t.next).toBe("TimerRecovery");
    expect(t.guard).toBe("info_field_length_le_N1_and_content_is_octet_aligned and va_le_nr_le_vs and own_receive_busy and not P_eq_1");
    expect(t.actions).toHaveLength(2);
    expect(t.actions[0].verb).toBe("Check_I_Frame_Acknowledged");
    expect(t.actions[0].kind).toBe("subroutine");
    expect(t.actions[1].verb).toBe("Discard Contents of I Frame");
    expect(t.actions[1].kind).toBe("processing");
  });

  it("t22_i_received_undefined_yes_yes_no_yes_no_yes", () => {
    const t = DataLinkTimerRecovery.transitions.find((x) => x.id === "t22_i_received_undefined_yes_yes_no_yes_no_yes");
    expect(t, "transition t22_i_received_undefined_yes_yes_no_yes_no_yes not found").toBeDefined();
    if (!t) return;
    expect(t.on).toBe("I_received");
    expect(t.next).toBe("TimerRecovery");
    expect(t.guard).toBe("info_field_length_le_N1_and_content_is_octet_aligned and va_le_nr_le_vs and not own_receive_busy and ns_eq_vr and P_eq_1");
    expect(t.actions).toHaveLength(9);
    expect(t.actions[0].verb).toBe("Check_I_Frame_Acknowledged");
    expect(t.actions[0].kind).toBe("subroutine");
    expect(t.actions[1].verb).toBe("V(r) := V(r) + 1");
    expect(t.actions[1].kind).toBe("processing");
    expect(t.actions[2].verb).toBe("Clear Reject Exception");
    expect(t.actions[2].kind).toBe("processing");
    expect(t.actions[3].verb).toBe("Decrement Sreject Exception if > 0");
    expect(t.actions[3].kind).toBe("processing");
    expect(t.actions[4].verb).toBe("DL-DATA Indication");
    expect(t.actions[4].kind).toBe("signal_upper");
    expect(t.actions[5].verb).toBe("F := 1");
    expect(t.actions[5].kind).toBe("processing");
    expect(t.actions[6].verb).toBe("N(r) := V(r)");
    expect(t.actions[6].kind).toBe("processing");
    expect(t.actions[7].verb).toBe("RR");
    expect(t.actions[7].kind).toBe("signal_lower");
    expect(t.actions[8].verb).toBe("Clear Acknowledge Pending");
    expect(t.actions[8].kind).toBe("processing");
  });

  it("t22_i_received_undefined_yes_yes_no_yes_no_no_no", () => {
    const t = DataLinkTimerRecovery.transitions.find((x) => x.id === "t22_i_received_undefined_yes_yes_no_yes_no_no_no");
    expect(t, "transition t22_i_received_undefined_yes_yes_no_yes_no_no_no not found").toBeDefined();
    if (!t) return;
    expect(t.on).toBe("I_received");
    expect(t.next).toBe("TimerRecovery");
    expect(t.guard).toBe("info_field_length_le_N1_and_content_is_octet_aligned and va_le_nr_le_vs and not own_receive_busy and ns_eq_vr and not P_eq_1 and not ack_pending");
    expect(t.actions).toHaveLength(7);
    expect(t.actions[0].verb).toBe("Check_I_Frame_Acknowledged");
    expect(t.actions[0].kind).toBe("subroutine");
    expect(t.actions[1].verb).toBe("V(r) := V(r) + 1");
    expect(t.actions[1].kind).toBe("processing");
    expect(t.actions[2].verb).toBe("Clear Reject Exception");
    expect(t.actions[2].kind).toBe("processing");
    expect(t.actions[3].verb).toBe("Decrement Sreject Exception if > 0");
    expect(t.actions[3].kind).toBe("processing");
    expect(t.actions[4].verb).toBe("DL-DATA Indication");
    expect(t.actions[4].kind).toBe("signal_upper");
    expect(t.actions[5].verb).toBe("Set Acknowledge Pending");
    expect(t.actions[5].kind).toBe("processing");
    expect(t.actions[6].verb).toBe("LM-SIEZE Request");
    expect(t.actions[6].kind).toBe("signal_lower");
  });

  it("t22_i_received_undefined_yes_yes_no_yes_no_no_yes", () => {
    const t = DataLinkTimerRecovery.transitions.find((x) => x.id === "t22_i_received_undefined_yes_yes_no_yes_no_no_yes");
    expect(t, "transition t22_i_received_undefined_yes_yes_no_yes_no_no_yes not found").toBeDefined();
    if (!t) return;
    expect(t.on).toBe("I_received");
    expect(t.next).toBe("TimerRecovery");
    expect(t.guard).toBe("info_field_length_le_N1_and_content_is_octet_aligned and va_le_nr_le_vs and not own_receive_busy and ns_eq_vr and not P_eq_1 and ack_pending");
    expect(t.actions).toHaveLength(5);
    expect(t.actions[0].verb).toBe("Check_I_Frame_Acknowledged");
    expect(t.actions[0].kind).toBe("subroutine");
    expect(t.actions[1].verb).toBe("V(r) := V(r) + 1");
    expect(t.actions[1].kind).toBe("processing");
    expect(t.actions[2].verb).toBe("Clear Reject Exception");
    expect(t.actions[2].kind).toBe("processing");
    expect(t.actions[3].verb).toBe("Decrement Sreject Exception if > 0");
    expect(t.actions[3].kind).toBe("processing");
    expect(t.actions[4].verb).toBe("DL-DATA Indication");
    expect(t.actions[4].kind).toBe("signal_upper");
  });

  it("t22_i_received_undefined_yes_yes_no_no_yes_yes", () => {
    const t = DataLinkTimerRecovery.transitions.find((x) => x.id === "t22_i_received_undefined_yes_yes_no_no_yes_yes");
    expect(t, "transition t22_i_received_undefined_yes_yes_no_no_yes_yes not found").toBeDefined();
    if (!t) return;
    expect(t.on).toBe("I_received");
    expect(t.next).toBe("TimerRecovery");
    expect(t.guard).toBe("info_field_length_le_N1_and_content_is_octet_aligned and va_le_nr_le_vs and not own_receive_busy and not ns_eq_vr and reject_exception and P_eq_1");
    expect(t.actions).toHaveLength(6);
    expect(t.actions[0].verb).toBe("Check_I_Frame_Acknowledged");
    expect(t.actions[0].kind).toBe("subroutine");
    expect(t.actions[1].verb).toBe("Discard Contents of I Frame");
    expect(t.actions[1].kind).toBe("processing");
    expect(t.actions[2].verb).toBe("F := 1");
    expect(t.actions[2].kind).toBe("processing");
    expect(t.actions[3].verb).toBe("N(r) := V(r)");
    expect(t.actions[3].kind).toBe("processing");
    expect(t.actions[4].verb).toBe("RR");
    expect(t.actions[4].kind).toBe("signal_lower");
    expect(t.actions[5].verb).toBe("Clear Acknowledge Pending");
    expect(t.actions[5].kind).toBe("processing");
  });

  it("t22_i_received_undefined_yes_yes_no_no_yes_no", () => {
    const t = DataLinkTimerRecovery.transitions.find((x) => x.id === "t22_i_received_undefined_yes_yes_no_no_yes_no");
    expect(t, "transition t22_i_received_undefined_yes_yes_no_no_yes_no not found").toBeDefined();
    if (!t) return;
    expect(t.on).toBe("I_received");
    expect(t.next).toBe("TimerRecovery");
    expect(t.guard).toBe("info_field_length_le_N1_and_content_is_octet_aligned and va_le_nr_le_vs and not own_receive_busy and not ns_eq_vr and reject_exception and not P_eq_1");
    expect(t.actions).toHaveLength(2);
    expect(t.actions[0].verb).toBe("Check_I_Frame_Acknowledged");
    expect(t.actions[0].kind).toBe("subroutine");
    expect(t.actions[1].verb).toBe("Discard Contents of I Frame");
    expect(t.actions[1].kind).toBe("processing");
  });

  it("t22_i_received_undefined_yes_yes_no_no_no_no", () => {
    const t = DataLinkTimerRecovery.transitions.find((x) => x.id === "t22_i_received_undefined_yes_yes_no_no_no_no");
    expect(t, "transition t22_i_received_undefined_yes_yes_no_no_no_no not found").toBeDefined();
    if (!t) return;
    expect(t.on).toBe("I_received");
    expect(t.next).toBe("TimerRecovery");
    expect(t.guard).toBe("info_field_length_le_N1_and_content_is_octet_aligned and va_le_nr_le_vs and not own_receive_busy and not ns_eq_vr and not reject_exception and not SREJ_enabled");
    expect(t.actions).toHaveLength(7);
    expect(t.actions[0].verb).toBe("Check_I_Frame_Acknowledged");
    expect(t.actions[0].kind).toBe("subroutine");
    expect(t.actions[1].verb).toBe("Discard Contents of I Frame");
    expect(t.actions[1].kind).toBe("processing");
    expect(t.actions[2].verb).toBe("Set Reject Exception");
    expect(t.actions[2].kind).toBe("processing");
    expect(t.actions[3].verb).toBe("F := P");
    expect(t.actions[3].kind).toBe("processing");
    expect(t.actions[4].verb).toBe("N(r) := V(r)");
    expect(t.actions[4].kind).toBe("processing");
    expect(t.actions[5].verb).toBe("REJ");
    expect(t.actions[5].kind).toBe("signal_lower");
    expect(t.actions[6].verb).toBe("Set Acknowledge Pending");
    expect(t.actions[6].kind).toBe("processing");
  });

  it("t22_i_received_undefined_yes_yes_no_no_no_yes_no_yes", () => {
    const t = DataLinkTimerRecovery.transitions.find((x) => x.id === "t22_i_received_undefined_yes_yes_no_no_no_yes_no_yes");
    expect(t, "transition t22_i_received_undefined_yes_yes_no_no_no_yes_no_yes not found").toBeDefined();
    if (!t) return;
    expect(t.on).toBe("I_received");
    expect(t.next).toBe("TimerRecovery");
    expect(t.guard).toBe("info_field_length_le_N1_and_content_is_octet_aligned and va_le_nr_le_vs and not own_receive_busy and not ns_eq_vr and not reject_exception and SREJ_enabled and not sreject_exception_gt_0 and ns_gt_vr_plus_1");
    expect(t.actions).toHaveLength(8);
    expect(t.actions[0].verb).toBe("Check_I_Frame_Acknowledged");
    expect(t.actions[0].kind).toBe("subroutine");
    expect(t.actions[1].verb).toBe("Save Contents of I Frame");
    expect(t.actions[1].kind).toBe("processing");
    expect(t.actions[2].verb).toBe("Discard Contents of I Frame");
    expect(t.actions[2].kind).toBe("processing");
    expect(t.actions[3].verb).toBe("Set Reject Exception");
    expect(t.actions[3].kind).toBe("processing");
    expect(t.actions[4].verb).toBe("F := P");
    expect(t.actions[4].kind).toBe("processing");
    expect(t.actions[5].verb).toBe("N(r) := V(r)");
    expect(t.actions[5].kind).toBe("processing");
    expect(t.actions[6].verb).toBe("REJ");
    expect(t.actions[6].kind).toBe("signal_lower");
    expect(t.actions[7].verb).toBe("Set Acknowledge Pending");
    expect(t.actions[7].kind).toBe("processing");
  });

  it("t22_i_received_undefined_yes_yes_no_no_no_yes_no_no", () => {
    const t = DataLinkTimerRecovery.transitions.find((x) => x.id === "t22_i_received_undefined_yes_yes_no_no_no_yes_no_no");
    expect(t, "transition t22_i_received_undefined_yes_yes_no_no_no_yes_no_no not found").toBeDefined();
    if (!t) return;
    expect(t.on).toBe("I_received");
    expect(t.next).toBe("TimerRecovery");
    expect(t.guard).toBe("info_field_length_le_N1_and_content_is_octet_aligned and va_le_nr_le_vs and not own_receive_busy and not ns_eq_vr and not reject_exception and SREJ_enabled and not sreject_exception_gt_0 and not ns_gt_vr_plus_1");
    expect(t.actions).toHaveLength(6);
    expect(t.actions[0].verb).toBe("Check_I_Frame_Acknowledged");
    expect(t.actions[0].kind).toBe("subroutine");
    expect(t.actions[1].verb).toBe("Save Contents of I Frame");
    expect(t.actions[1].kind).toBe("processing");
    expect(t.actions[2].verb).toBe("N(r) := V(r)");
    expect(t.actions[2].kind).toBe("processing");
    expect(t.actions[3].verb).toBe("F := 1");
    expect(t.actions[3].kind).toBe("processing");
    expect(t.actions[4].verb).toBe("Sreject := Sreject + 1");
    expect(t.actions[4].kind).toBe("processing");
    expect(t.actions[5].verb).toBe("SREJ");
    expect(t.actions[5].kind).toBe("signal_lower");
  });

  it("t22_i_received_undefined_yes_yes_no_no_no_yes_yes", () => {
    const t = DataLinkTimerRecovery.transitions.find((x) => x.id === "t22_i_received_undefined_yes_yes_no_no_no_yes_yes");
    expect(t, "transition t22_i_received_undefined_yes_yes_no_no_no_yes_yes not found").toBeDefined();
    if (!t) return;
    expect(t.on).toBe("I_received");
    expect(t.next).toBe("TimerRecovery");
    expect(t.guard).toBe("info_field_length_le_N1_and_content_is_octet_aligned and va_le_nr_le_vs and not own_receive_busy and not ns_eq_vr and not reject_exception and SREJ_enabled and sreject_exception_gt_0");
    expect(t.actions).toHaveLength(6);
    expect(t.actions[0].verb).toBe("Check_I_Frame_Acknowledged");
    expect(t.actions[0].kind).toBe("subroutine");
    expect(t.actions[1].verb).toBe("Save Contents of I Frame");
    expect(t.actions[1].kind).toBe("processing");
    expect(t.actions[2].verb).toBe("N(r) := V(r)");
    expect(t.actions[2].kind).toBe("processing");
    expect(t.actions[3].verb).toBe("F := 0");
    expect(t.actions[3].kind).toBe("processing");
    expect(t.actions[4].verb).toBe("Sreject := Sreject + 1");
    expect(t.actions[4].kind).toBe("processing");
    expect(t.actions[5].verb).toBe("SREJ");
    expect(t.actions[5].kind).toBe("signal_lower");
  });

  it("t23_rej_received_no_yes_yes_yes", () => {
    const t = DataLinkTimerRecovery.transitions.find((x) => x.id === "t23_rej_received_no_yes_yes_yes");
    expect(t, "transition t23_rej_received_no_yes_yes_yes not found").toBeDefined();
    if (!t) return;
    expect(t.on).toBe("REJ_received");
    expect(t.next).toBe("TimerRecovery");
    expect(t.guard).toBe("not response_and_F_eq_1 and command_and_P_eq_1 and va_le_nr_le_vs and vs_eq_va");
    expect(t.actions).toHaveLength(3);
    expect(t.actions[0].verb).toBe("clear_peer_receiver_busy");
    expect(t.actions[0].kind).toBe("processing");
    expect(t.actions[1].verb).toBe("Enquiry_Response_F_1");
    expect(t.actions[1].kind).toBe("subroutine");
    expect(t.actions[2].verb).toBe("V(a) := N(r)");
    expect(t.actions[2].kind).toBe("processing");
  });

  it("t23_rej_received_no_yes_yes_no", () => {
    const t = DataLinkTimerRecovery.transitions.find((x) => x.id === "t23_rej_received_no_yes_yes_no");
    expect(t, "transition t23_rej_received_no_yes_yes_no not found").toBeDefined();
    if (!t) return;
    expect(t.on).toBe("REJ_received");
    expect(t.next).toBe("TimerRecovery");
    expect(t.guard).toBe("not response_and_F_eq_1 and command_and_P_eq_1 and va_le_nr_le_vs and not vs_eq_va");
    expect(t.actions).toHaveLength(7);
    expect(t.actions[0].verb).toBe("clear_peer_receiver_busy");
    expect(t.actions[0].kind).toBe("processing");
    expect(t.actions[1].verb).toBe("Enquiry_Response_F_1");
    expect(t.actions[1].kind).toBe("subroutine");
    expect(t.actions[2].verb).toBe("V(a) := N(r)");
    expect(t.actions[2].kind).toBe("processing");
    expect(t.actions[3].verb).toBe("Invoke Retransmission");
    expect(t.actions[3].kind).toBe("subroutine");
    expect(t.actions[4].verb).toBe("Stop T3");
    expect(t.actions[4].kind).toBe("processing");
    expect(t.actions[5].verb).toBe("Start T1");
    expect(t.actions[5].kind).toBe("processing");
    expect(t.actions[6].verb).toBe("Clear Acknowledge Pending");
    expect(t.actions[6].kind).toBe("processing");
  });

  it("t23_rej_received_no_yes_no_yes", () => {
    const t = DataLinkTimerRecovery.transitions.find((x) => x.id === "t23_rej_received_no_yes_no_yes");
    expect(t, "transition t23_rej_received_no_yes_no_yes not found").toBeDefined();
    if (!t) return;
    expect(t.on).toBe("REJ_received");
    expect(t.next).toBe("AwaitingV22Connection");
    expect(t.guard).toBe("not response_and_F_eq_1 and command_and_P_eq_1 and not va_le_nr_le_vs and version_2_2");
    expect(t.actions).toHaveLength(3);
    expect(t.actions[0].verb).toBe("clear_peer_receiver_busy");
    expect(t.actions[0].kind).toBe("processing");
    expect(t.actions[1].verb).toBe("Enquiry_Response_F_1");
    expect(t.actions[1].kind).toBe("subroutine");
    expect(t.actions[2].verb).toBe("N(r) Recovery");
    expect(t.actions[2].kind).toBe("subroutine");
  });

  it("t23_rej_received_no_yes_no_no", () => {
    const t = DataLinkTimerRecovery.transitions.find((x) => x.id === "t23_rej_received_no_yes_no_no");
    expect(t, "transition t23_rej_received_no_yes_no_no not found").toBeDefined();
    if (!t) return;
    expect(t.on).toBe("REJ_received");
    expect(t.next).toBe("AwaitingConnection");
    expect(t.guard).toBe("not response_and_F_eq_1 and command_and_P_eq_1 and not va_le_nr_le_vs and not version_2_2");
    expect(t.actions).toHaveLength(3);
    expect(t.actions[0].verb).toBe("clear_peer_receiver_busy");
    expect(t.actions[0].kind).toBe("processing");
    expect(t.actions[1].verb).toBe("Enquiry_Response_F_1");
    expect(t.actions[1].kind).toBe("subroutine");
    expect(t.actions[2].verb).toBe("N(r) Recovery");
    expect(t.actions[2].kind).toBe("subroutine");
  });

  it("t23_rej_received_no_no_yes_yes", () => {
    const t = DataLinkTimerRecovery.transitions.find((x) => x.id === "t23_rej_received_no_no_yes_yes");
    expect(t, "transition t23_rej_received_no_no_yes_yes not found").toBeDefined();
    if (!t) return;
    expect(t.on).toBe("REJ_received");
    expect(t.next).toBe("TimerRecovery");
    expect(t.guard).toBe("not response_and_F_eq_1 and not command_and_P_eq_1 and va_le_nr_le_vs and vs_eq_va");
    expect(t.actions).toHaveLength(2);
    expect(t.actions[0].verb).toBe("clear_peer_receiver_busy");
    expect(t.actions[0].kind).toBe("processing");
    expect(t.actions[1].verb).toBe("V(a) := N(r)");
    expect(t.actions[1].kind).toBe("processing");
  });

  it("t23_rej_received_no_no_yes_no", () => {
    const t = DataLinkTimerRecovery.transitions.find((x) => x.id === "t23_rej_received_no_no_yes_no");
    expect(t, "transition t23_rej_received_no_no_yes_no not found").toBeDefined();
    if (!t) return;
    expect(t.on).toBe("REJ_received");
    expect(t.next).toBe("TimerRecovery");
    expect(t.guard).toBe("not response_and_F_eq_1 and not command_and_P_eq_1 and va_le_nr_le_vs and not vs_eq_va");
    expect(t.actions).toHaveLength(6);
    expect(t.actions[0].verb).toBe("clear_peer_receiver_busy");
    expect(t.actions[0].kind).toBe("processing");
    expect(t.actions[1].verb).toBe("V(a) := N(r)");
    expect(t.actions[1].kind).toBe("processing");
    expect(t.actions[2].verb).toBe("Invoke Retransmission");
    expect(t.actions[2].kind).toBe("subroutine");
    expect(t.actions[3].verb).toBe("Stop T3");
    expect(t.actions[3].kind).toBe("processing");
    expect(t.actions[4].verb).toBe("Start T1");
    expect(t.actions[4].kind).toBe("processing");
    expect(t.actions[5].verb).toBe("Clear Acknowledge Pending");
    expect(t.actions[5].kind).toBe("processing");
  });

  it("t23_rej_received_no_no_no_yes", () => {
    const t = DataLinkTimerRecovery.transitions.find((x) => x.id === "t23_rej_received_no_no_no_yes");
    expect(t, "transition t23_rej_received_no_no_no_yes not found").toBeDefined();
    if (!t) return;
    expect(t.on).toBe("REJ_received");
    expect(t.next).toBe("AwaitingV22Connection");
    expect(t.guard).toBe("not response_and_F_eq_1 and not command_and_P_eq_1 and not va_le_nr_le_vs and version_2_2");
    expect(t.actions).toHaveLength(2);
    expect(t.actions[0].verb).toBe("clear_peer_receiver_busy");
    expect(t.actions[0].kind).toBe("processing");
    expect(t.actions[1].verb).toBe("N(r) Recovery");
    expect(t.actions[1].kind).toBe("subroutine");
  });

  it("t23_rej_received_no_no_no_no", () => {
    const t = DataLinkTimerRecovery.transitions.find((x) => x.id === "t23_rej_received_no_no_no_no");
    expect(t, "transition t23_rej_received_no_no_no_no not found").toBeDefined();
    if (!t) return;
    expect(t.on).toBe("REJ_received");
    expect(t.next).toBe("AwaitingConnection");
    expect(t.guard).toBe("not response_and_F_eq_1 and not command_and_P_eq_1 and not va_le_nr_le_vs and not version_2_2");
    expect(t.actions).toHaveLength(2);
    expect(t.actions[0].verb).toBe("clear_peer_receiver_busy");
    expect(t.actions[0].kind).toBe("processing");
    expect(t.actions[1].verb).toBe("N(r) Recovery");
    expect(t.actions[1].kind).toBe("subroutine");
  });

  it("t23_rej_received_yes_no_yes", () => {
    const t = DataLinkTimerRecovery.transitions.find((x) => x.id === "t23_rej_received_yes_no_yes");
    expect(t, "transition t23_rej_received_yes_no_yes not found").toBeDefined();
    if (!t) return;
    expect(t.on).toBe("REJ_received");
    expect(t.next).toBe("AwaitingV22Connection");
    expect(t.guard).toBe("response_and_F_eq_1 and not va_le_nr_le_vs and version_2_2");
    expect(t.actions).toHaveLength(4);
    expect(t.actions[0].verb).toBe("clear_peer_receiver_busy");
    expect(t.actions[0].kind).toBe("processing");
    expect(t.actions[1].verb).toBe("Stop T1");
    expect(t.actions[1].kind).toBe("processing");
    expect(t.actions[2].verb).toBe("Select_T1_Value");
    expect(t.actions[2].kind).toBe("subroutine");
    expect(t.actions[3].verb).toBe("N(r) Recovery");
    expect(t.actions[3].kind).toBe("subroutine");
  });

  it("t23_rej_received_yes_no_no", () => {
    const t = DataLinkTimerRecovery.transitions.find((x) => x.id === "t23_rej_received_yes_no_no");
    expect(t, "transition t23_rej_received_yes_no_no not found").toBeDefined();
    if (!t) return;
    expect(t.on).toBe("REJ_received");
    expect(t.next).toBe("AwaitingConnection");
    expect(t.guard).toBe("response_and_F_eq_1 and not va_le_nr_le_vs and not version_2_2");
    expect(t.actions).toHaveLength(4);
    expect(t.actions[0].verb).toBe("clear_peer_receiver_busy");
    expect(t.actions[0].kind).toBe("processing");
    expect(t.actions[1].verb).toBe("Stop T1");
    expect(t.actions[1].kind).toBe("processing");
    expect(t.actions[2].verb).toBe("Select_T1_Value");
    expect(t.actions[2].kind).toBe("subroutine");
    expect(t.actions[3].verb).toBe("N(r) Recovery");
    expect(t.actions[3].kind).toBe("subroutine");
  });

  it("t23_rej_received_yes_yes_undefined_via_start_t3", () => {
    const t = DataLinkTimerRecovery.transitions.find((x) => x.id === "t23_rej_received_yes_yes_undefined_via_start_t3");
    expect(t, "transition t23_rej_received_yes_yes_undefined_via_start_t3 not found").toBeDefined();
    if (!t) return;
    expect(t.on).toBe("REJ_received");
    expect(t.next).toBe("Connected");
    expect(t.guard).toBe("response_and_F_eq_1 and va_le_nr_le_vs");
    expect(t.actions).toHaveLength(6);
    expect(t.actions[0].verb).toBe("clear_peer_receiver_busy");
    expect(t.actions[0].kind).toBe("processing");
    expect(t.actions[1].verb).toBe("Stop T1");
    expect(t.actions[1].kind).toBe("processing");
    expect(t.actions[2].verb).toBe("Select_T1_Value");
    expect(t.actions[2].kind).toBe("subroutine");
    expect(t.actions[3].verb).toBe("V(a) := N(r)");
    expect(t.actions[3].kind).toBe("processing");
    expect(t.actions[4].verb).toBe("Start T3");
    expect(t.actions[4].kind).toBe("processing");
    expect(t.actions[5].verb).toBe("RC := 0");
    expect(t.actions[5].kind).toBe("processing");
  });

  it("t23_rej_received_yes_yes_undefined_via_invoke_retransmission", () => {
    const t = DataLinkTimerRecovery.transitions.find((x) => x.id === "t23_rej_received_yes_yes_undefined_via_invoke_retransmission");
    expect(t, "transition t23_rej_received_yes_yes_undefined_via_invoke_retransmission not found").toBeDefined();
    if (!t) return;
    expect(t.on).toBe("REJ_received");
    expect(t.next).toBe("TimerRecovery");
    expect(t.guard).toBe("response_and_F_eq_1 and va_le_nr_le_vs");
    expect(t.actions).toHaveLength(8);
    expect(t.actions[0].verb).toBe("clear_peer_receiver_busy");
    expect(t.actions[0].kind).toBe("processing");
    expect(t.actions[1].verb).toBe("Stop T1");
    expect(t.actions[1].kind).toBe("processing");
    expect(t.actions[2].verb).toBe("Select_T1_Value");
    expect(t.actions[2].kind).toBe("subroutine");
    expect(t.actions[3].verb).toBe("V(a) := N(r)");
    expect(t.actions[3].kind).toBe("processing");
    expect(t.actions[4].verb).toBe("Invoke Retransmission");
    expect(t.actions[4].kind).toBe("subroutine");
    expect(t.actions[5].verb).toBe("Stop T3");
    expect(t.actions[5].kind).toBe("processing");
    expect(t.actions[6].verb).toBe("Start T1");
    expect(t.actions[6].kind).toBe("processing");
    expect(t.actions[7].verb).toBe("Clear Acknowledge Pending");
    expect(t.actions[7].kind).toBe("processing");
  });

  it("t24_srej_received_no_yes_yes_yes", () => {
    const t = DataLinkTimerRecovery.transitions.find((x) => x.id === "t24_srej_received_no_yes_yes_yes");
    expect(t, "transition t24_srej_received_no_yes_yes_yes not found").toBeDefined();
    if (!t) return;
    expect(t.on).toBe("SREJ_received");
    expect(t.next).toBe("TimerRecovery");
    expect(t.guard).toBe("not response and va_le_nr_le_vs and P_eq_1 and vs_eq_va");
    expect(t.actions).toHaveLength(3);
    expect(t.actions[0].verb).toBe("clear_peer_receiver_busy");
    expect(t.actions[0].kind).toBe("processing");
    expect(t.actions[1].verb).toBe("V(a) := N(r)");
    expect(t.actions[1].kind).toBe("processing");
    expect(t.actions[2].verb).toBe("Check I Frame Acknowledged");
    expect(t.actions[2].kind).toBe("subroutine");
  });

  it("t24_srej_received_no_yes_yes_no", () => {
    const t = DataLinkTimerRecovery.transitions.find((x) => x.id === "t24_srej_received_no_yes_yes_no");
    expect(t, "transition t24_srej_received_no_yes_yes_no not found").toBeDefined();
    if (!t) return;
    expect(t.on).toBe("SREJ_received");
    expect(t.next).toBe("TimerRecovery");
    expect(t.guard).toBe("not response and va_le_nr_le_vs and P_eq_1 and not vs_eq_va");
    expect(t.actions).toHaveLength(4);
    expect(t.actions[0].verb).toBe("clear_peer_receiver_busy");
    expect(t.actions[0].kind).toBe("processing");
    expect(t.actions[1].verb).toBe("V(a) := N(r)");
    expect(t.actions[1].kind).toBe("processing");
    expect(t.actions[2].verb).toBe("Check I Frame Acknowledged");
    expect(t.actions[2].kind).toBe("subroutine");
    expect(t.actions[3].verb).toBe("Invoke Retransmission");
    expect(t.actions[3].kind).toBe("subroutine");
  });

  it("t24_srej_received_no_yes_no_yes", () => {
    const t = DataLinkTimerRecovery.transitions.find((x) => x.id === "t24_srej_received_no_yes_no_yes");
    expect(t, "transition t24_srej_received_no_yes_no_yes not found").toBeDefined();
    if (!t) return;
    expect(t.on).toBe("SREJ_received");
    expect(t.next).toBe("TimerRecovery");
    expect(t.guard).toBe("not response and va_le_nr_le_vs and not P_eq_1 and vs_eq_va");
    expect(t.actions).toHaveLength(1);
    expect(t.actions[0].verb).toBe("clear_peer_receiver_busy");
    expect(t.actions[0].kind).toBe("processing");
  });

  it("t24_srej_received_no_yes_no_no", () => {
    const t = DataLinkTimerRecovery.transitions.find((x) => x.id === "t24_srej_received_no_yes_no_no");
    expect(t, "transition t24_srej_received_no_yes_no_no not found").toBeDefined();
    if (!t) return;
    expect(t.on).toBe("SREJ_received");
    expect(t.next).toBe("TimerRecovery");
    expect(t.guard).toBe("not response and va_le_nr_le_vs and not P_eq_1 and not vs_eq_va");
    expect(t.actions).toHaveLength(2);
    expect(t.actions[0].verb).toBe("clear_peer_receiver_busy");
    expect(t.actions[0].kind).toBe("processing");
    expect(t.actions[1].verb).toBe("Invoke Retransmission");
    expect(t.actions[1].kind).toBe("subroutine");
  });

  it("t24_srej_received_no_no_no", () => {
    const t = DataLinkTimerRecovery.transitions.find((x) => x.id === "t24_srej_received_no_no_no");
    expect(t, "transition t24_srej_received_no_no_no not found").toBeDefined();
    if (!t) return;
    expect(t.on).toBe("SREJ_received");
    expect(t.next).toBe("AwaitingConnection");
    expect(t.guard).toBe("not response and not va_le_nr_le_vs and not version_2_2");
    expect(t.actions).toHaveLength(2);
    expect(t.actions[0].verb).toBe("clear_peer_receiver_busy");
    expect(t.actions[0].kind).toBe("processing");
    expect(t.actions[1].verb).toBe("N(r) Recovery");
    expect(t.actions[1].kind).toBe("subroutine");
  });

  it("t24_srej_received_no_no_yes", () => {
    const t = DataLinkTimerRecovery.transitions.find((x) => x.id === "t24_srej_received_no_no_yes");
    expect(t, "transition t24_srej_received_no_no_yes not found").toBeDefined();
    if (!t) return;
    expect(t.on).toBe("SREJ_received");
    expect(t.next).toBe("AwaitingV22Connection");
    expect(t.guard).toBe("not response and not va_le_nr_le_vs and version_2_2");
    expect(t.actions).toHaveLength(2);
    expect(t.actions[0].verb).toBe("clear_peer_receiver_busy");
    expect(t.actions[0].kind).toBe("processing");
    expect(t.actions[1].verb).toBe("N(r) Recovery");
    expect(t.actions[1].kind).toBe("subroutine");
  });

  it("t24_srej_received_yes_no_no", () => {
    const t = DataLinkTimerRecovery.transitions.find((x) => x.id === "t24_srej_received_yes_no_no");
    expect(t, "transition t24_srej_received_yes_no_no not found").toBeDefined();
    if (!t) return;
    expect(t.on).toBe("SREJ_received");
    expect(t.next).toBe("AwaitingConnection");
    expect(t.guard).toBe("response and not va_le_nr_le_vs and not version_2_2");
    expect(t.actions).toHaveLength(4);
    expect(t.actions[0].verb).toBe("clear_peer_receiver_busy");
    expect(t.actions[0].kind).toBe("processing");
    expect(t.actions[1].verb).toBe("Stop T1");
    expect(t.actions[1].kind).toBe("processing");
    expect(t.actions[2].verb).toBe("Select_T1_Value");
    expect(t.actions[2].kind).toBe("subroutine");
    expect(t.actions[3].verb).toBe("N(r) Recovery");
    expect(t.actions[3].kind).toBe("subroutine");
  });

  it("t24_srej_received_yes_no_yes", () => {
    const t = DataLinkTimerRecovery.transitions.find((x) => x.id === "t24_srej_received_yes_no_yes");
    expect(t, "transition t24_srej_received_yes_no_yes not found").toBeDefined();
    if (!t) return;
    expect(t.on).toBe("SREJ_received");
    expect(t.next).toBe("AwaitingV22Connection");
    expect(t.guard).toBe("response and not va_le_nr_le_vs and version_2_2");
    expect(t.actions).toHaveLength(4);
    expect(t.actions[0].verb).toBe("clear_peer_receiver_busy");
    expect(t.actions[0].kind).toBe("processing");
    expect(t.actions[1].verb).toBe("Stop T1");
    expect(t.actions[1].kind).toBe("processing");
    expect(t.actions[2].verb).toBe("Select_T1_Value");
    expect(t.actions[2].kind).toBe("subroutine");
    expect(t.actions[3].verb).toBe("N(r) Recovery");
    expect(t.actions[3].kind).toBe("subroutine");
  });

  it("t24_srej_received_yes_yes_yes_yes", () => {
    const t = DataLinkTimerRecovery.transitions.find((x) => x.id === "t24_srej_received_yes_yes_yes_yes");
    expect(t, "transition t24_srej_received_yes_yes_yes_yes not found").toBeDefined();
    if (!t) return;
    expect(t.on).toBe("SREJ_received");
    expect(t.next).toBe("Connected");
    expect(t.guard).toBe("response and va_le_nr_le_vs and F_eq_1 and vs_eq_va");
    expect(t.actions).toHaveLength(7);
    expect(t.actions[0].verb).toBe("clear_peer_receiver_busy");
    expect(t.actions[0].kind).toBe("processing");
    expect(t.actions[1].verb).toBe("Stop T1");
    expect(t.actions[1].kind).toBe("processing");
    expect(t.actions[2].verb).toBe("Select_T1_Value");
    expect(t.actions[2].kind).toBe("subroutine");
    expect(t.actions[3].verb).toBe("V(a) := N(r)");
    expect(t.actions[3].kind).toBe("processing");
    expect(t.actions[4].verb).toBe("Check I Frame Acknowledged");
    expect(t.actions[4].kind).toBe("subroutine");
    expect(t.actions[5].verb).toBe("Start T3");
    expect(t.actions[5].kind).toBe("processing");
    expect(t.actions[6].verb).toBe("RC := 0");
    expect(t.actions[6].kind).toBe("processing");
  });

  it("t24_srej_received_yes_yes_yes_no", () => {
    const t = DataLinkTimerRecovery.transitions.find((x) => x.id === "t24_srej_received_yes_yes_yes_no");
    expect(t, "transition t24_srej_received_yes_yes_yes_no not found").toBeDefined();
    if (!t) return;
    expect(t.on).toBe("SREJ_received");
    expect(t.next).toBe("TimerRecovery");
    expect(t.guard).toBe("response and va_le_nr_le_vs and F_eq_1 and not vs_eq_va");
    expect(t.actions).toHaveLength(11);
    expect(t.actions[0].verb).toBe("clear_peer_receiver_busy");
    expect(t.actions[0].kind).toBe("processing");
    expect(t.actions[1].verb).toBe("Stop T1");
    expect(t.actions[1].kind).toBe("processing");
    expect(t.actions[2].verb).toBe("Select_T1_Value");
    expect(t.actions[2].kind).toBe("subroutine");
    expect(t.actions[3].verb).toBe("V(a) := N(r)");
    expect(t.actions[3].kind).toBe("processing");
    expect(t.actions[4].verb).toBe("Check I Frame Acknowledged");
    expect(t.actions[4].kind).toBe("subroutine");
    expect(t.actions[5].verb).toBe("push_frame_on_queue");
    expect(t.actions[5].kind).toBe("internal_out");
    expect(t.actions[6].verb).toBe("LM-DATA Request");
    expect(t.actions[6].kind).toBe("signal_lower");
    expect(t.actions[7].verb).toBe("Stop T3");
    expect(t.actions[7].kind).toBe("processing");
    expect(t.actions[8].verb).toBe("Start T1");
    expect(t.actions[8].kind).toBe("processing");
    expect(t.actions[9].verb).toBe("Clear Acknowledge Pending");
    expect(t.actions[9].kind).toBe("processing");
    expect(t.actions[10].verb).toBe("Invoke Retransmission");
    expect(t.actions[10].kind).toBe("subroutine");
  });

  it("t24_srej_received_yes_yes_no_yes", () => {
    const t = DataLinkTimerRecovery.transitions.find((x) => x.id === "t24_srej_received_yes_yes_no_yes");
    expect(t, "transition t24_srej_received_yes_yes_no_yes not found").toBeDefined();
    if (!t) return;
    expect(t.on).toBe("SREJ_received");
    expect(t.next).toBe("Connected");
    expect(t.guard).toBe("response and va_le_nr_le_vs and not F_eq_1 and vs_eq_va");
    expect(t.actions).toHaveLength(5);
    expect(t.actions[0].verb).toBe("clear_peer_receiver_busy");
    expect(t.actions[0].kind).toBe("processing");
    expect(t.actions[1].verb).toBe("Stop T1");
    expect(t.actions[1].kind).toBe("processing");
    expect(t.actions[2].verb).toBe("Select_T1_Value");
    expect(t.actions[2].kind).toBe("subroutine");
    expect(t.actions[3].verb).toBe("Start T3");
    expect(t.actions[3].kind).toBe("processing");
    expect(t.actions[4].verb).toBe("RC := 0");
    expect(t.actions[4].kind).toBe("processing");
  });

  it("t24_srej_received_yes_yes_no_no", () => {
    const t = DataLinkTimerRecovery.transitions.find((x) => x.id === "t24_srej_received_yes_yes_no_no");
    expect(t, "transition t24_srej_received_yes_yes_no_no not found").toBeDefined();
    if (!t) return;
    expect(t.on).toBe("SREJ_received");
    expect(t.next).toBe("TimerRecovery");
    expect(t.guard).toBe("response and va_le_nr_le_vs and not F_eq_1 and not vs_eq_va");
    expect(t.actions).toHaveLength(9);
    expect(t.actions[0].verb).toBe("clear_peer_receiver_busy");
    expect(t.actions[0].kind).toBe("processing");
    expect(t.actions[1].verb).toBe("Stop T1");
    expect(t.actions[1].kind).toBe("processing");
    expect(t.actions[2].verb).toBe("Select_T1_Value");
    expect(t.actions[2].kind).toBe("subroutine");
    expect(t.actions[3].verb).toBe("push_frame_on_queue");
    expect(t.actions[3].kind).toBe("internal_out");
    expect(t.actions[4].verb).toBe("LM-DATA Request");
    expect(t.actions[4].kind).toBe("signal_lower");
    expect(t.actions[5].verb).toBe("Stop T3");
    expect(t.actions[5].kind).toBe("processing");
    expect(t.actions[6].verb).toBe("Start T1");
    expect(t.actions[6].kind).toBe("processing");
    expect(t.actions[7].verb).toBe("Clear Acknowledge Pending");
    expect(t.actions[7].kind).toBe("processing");
    expect(t.actions[8].verb).toBe("Invoke Retransmission");
    expect(t.actions[8].kind).toBe("subroutine");
  });

});
