// Code generated by codegen/src/Packet.Sdl.CodeGen from spec-sdl/v2.2-errata/data-link/yaml/awaiting_v22_connection.sdl.yaml.
// DO NOT EDIT. Run `dotnet run --project codegen/src/Packet.Sdl.CodeGen` to regenerate.

#include "ax25sdl.h"
#include <stdio.h>
#include <string.h>

extern const StatePage data_link_awaiting_v22_connection;

#define ASSERT(cond, msg) do { if (!(cond)) { fprintf(stderr, "FAIL: %s\n", msg); return 1; } } while (0)
#define ASSERT_STREQ(a, b, msg) ASSERT(strcmp((a), (b)) == 0, msg)

static int test_source_figure(void) {
    ASSERT_STREQ(data_link_awaiting_v22_connection.source.figure, "figc4.6", "source.figure");
    return 0;
}

static int test_transitions_count(void) {
    ASSERT(data_link_awaiting_v22_connection.transitions_len == 25, "transitions count");
    return 0;
}

static int test_t02_dl_connect_request(void) {
    const TransitionSpec* t = NULL;
    for (size_t i = 0; i < data_link_awaiting_v22_connection.transitions_len; i++) {
        if (strcmp(data_link_awaiting_v22_connection.transitions[i].id, "t02_dl_connect_request") == 0) {
            t = &data_link_awaiting_v22_connection.transitions[i];
            break;
        }
    }
    ASSERT(t != NULL, "t02_dl_connect_request not found");
    ASSERT_STREQ(t->on, "DL_CONNECT_request", "on");
    ASSERT_STREQ(t->next, "AwaitingV22Connection", "next");
    ASSERT(t->actions_len == 2, "actions count");
    ASSERT_STREQ(t->actions[0].verb, "Discard Queue", "actions[0].verb");
    ASSERT(t->actions[0].kind == AX25SDL_KIND_PROCESSING, "actions[0].kind");
    ASSERT_STREQ(t->actions[1].verb, "Set Layer 3 Initiated", "actions[1].verb");
    ASSERT(t->actions[1].kind == AX25SDL_KIND_PROCESSING, "actions[1].kind");
    return 0;
}

static int test_t03_dl_unit_data_request(void) {
    const TransitionSpec* t = NULL;
    for (size_t i = 0; i < data_link_awaiting_v22_connection.transitions_len; i++) {
        if (strcmp(data_link_awaiting_v22_connection.transitions[i].id, "t03_dl_unit_data_request") == 0) {
            t = &data_link_awaiting_v22_connection.transitions[i];
            break;
        }
    }
    ASSERT(t != NULL, "t03_dl_unit_data_request not found");
    ASSERT_STREQ(t->on, "DL_UNIT_DATA_request", "on");
    ASSERT_STREQ(t->next, "AwaitingV22Connection", "next");
    ASSERT(t->actions_len == 1, "actions count");
    ASSERT_STREQ(t->actions[0].verb, "UI Command", "actions[0].verb");
    ASSERT(t->actions[0].kind == AX25SDL_KIND_SIGNAL_LOWER, "actions[0].kind");
    return 0;
}

static int test_t04_dl_data_request_yes(void) {
    const TransitionSpec* t = NULL;
    for (size_t i = 0; i < data_link_awaiting_v22_connection.transitions_len; i++) {
        if (strcmp(data_link_awaiting_v22_connection.transitions[i].id, "t04_dl_data_request_yes") == 0) {
            t = &data_link_awaiting_v22_connection.transitions[i];
            break;
        }
    }
    ASSERT(t != NULL, "t04_dl_data_request_yes not found");
    ASSERT_STREQ(t->on, "DL_DATA_request", "on");
    ASSERT_STREQ(t->next, "AwaitingV22Connection", "next");
    ASSERT_STREQ(t->guard, "layer_3_initiated", "guard");
    ASSERT(t->actions_len == 0, "actions count");
    return 0;
}

static int test_t04_dl_data_request_no(void) {
    const TransitionSpec* t = NULL;
    for (size_t i = 0; i < data_link_awaiting_v22_connection.transitions_len; i++) {
        if (strcmp(data_link_awaiting_v22_connection.transitions[i].id, "t04_dl_data_request_no") == 0) {
            t = &data_link_awaiting_v22_connection.transitions[i];
            break;
        }
    }
    ASSERT(t != NULL, "t04_dl_data_request_no not found");
    ASSERT_STREQ(t->on, "DL_DATA_request", "on");
    ASSERT_STREQ(t->next, "AwaitingV22Connection", "next");
    ASSERT_STREQ(t->guard, "not layer_3_initiated", "guard");
    ASSERT(t->actions_len == 1, "actions count");
    ASSERT_STREQ(t->actions[0].verb, "push_frame_on_queue", "actions[0].verb");
    ASSERT(t->actions[0].kind == AX25SDL_KIND_INTERNAL_OUT, "actions[0].kind");
    return 0;
}

static int test_t05_i_frame_pops_off_queue_yes(void) {
    const TransitionSpec* t = NULL;
    for (size_t i = 0; i < data_link_awaiting_v22_connection.transitions_len; i++) {
        if (strcmp(data_link_awaiting_v22_connection.transitions[i].id, "t05_i_frame_pops_off_queue_yes") == 0) {
            t = &data_link_awaiting_v22_connection.transitions[i];
            break;
        }
    }
    ASSERT(t != NULL, "t05_i_frame_pops_off_queue_yes not found");
    ASSERT_STREQ(t->on, "I_frame_pops_off_queue", "on");
    ASSERT_STREQ(t->next, "AwaitingV22Connection", "next");
    ASSERT_STREQ(t->guard, "layer_3_initiated", "guard");
    ASSERT(t->actions_len == 0, "actions count");
    return 0;
}

static int test_t05_i_frame_pops_off_queue_no(void) {
    const TransitionSpec* t = NULL;
    for (size_t i = 0; i < data_link_awaiting_v22_connection.transitions_len; i++) {
        if (strcmp(data_link_awaiting_v22_connection.transitions[i].id, "t05_i_frame_pops_off_queue_no") == 0) {
            t = &data_link_awaiting_v22_connection.transitions[i];
            break;
        }
    }
    ASSERT(t != NULL, "t05_i_frame_pops_off_queue_no not found");
    ASSERT_STREQ(t->on, "I_frame_pops_off_queue", "on");
    ASSERT_STREQ(t->next, "AwaitingV22Connection", "next");
    ASSERT_STREQ(t->guard, "not layer_3_initiated", "guard");
    ASSERT(t->actions_len == 1, "actions count");
    ASSERT_STREQ(t->actions[0].verb, "push_frame_on_queue", "actions[0].verb");
    ASSERT(t->actions[0].kind == AX25SDL_KIND_INTERNAL_OUT, "actions[0].kind");
    return 0;
}

static int test_t06_all_other_primitives__from_upper_layer(void) {
    const TransitionSpec* t = NULL;
    for (size_t i = 0; i < data_link_awaiting_v22_connection.transitions_len; i++) {
        if (strcmp(data_link_awaiting_v22_connection.transitions[i].id, "t06_all_other_primitives__from_upper_layer") == 0) {
            t = &data_link_awaiting_v22_connection.transitions[i];
            break;
        }
    }
    ASSERT(t != NULL, "t06_all_other_primitives__from_upper_layer not found");
    ASSERT_STREQ(t->on, "all_other_primitives__from_upper_layer", "on");
    ASSERT_STREQ(t->next, "AwaitingV22Connection", "next");
    ASSERT(t->actions_len == 0, "actions count");
    return 0;
}

static int test_t07_control_field_error(void) {
    const TransitionSpec* t = NULL;
    for (size_t i = 0; i < data_link_awaiting_v22_connection.transitions_len; i++) {
        if (strcmp(data_link_awaiting_v22_connection.transitions[i].id, "t07_control_field_error") == 0) {
            t = &data_link_awaiting_v22_connection.transitions[i];
            break;
        }
    }
    ASSERT(t != NULL, "t07_control_field_error not found");
    ASSERT_STREQ(t->on, "control_field_error", "on");
    ASSERT_STREQ(t->next, "AwaitingV22Connection", "next");
    ASSERT(t->actions_len == 1, "actions count");
    ASSERT_STREQ(t->actions[0].verb, "DL-ERROR Indication (L)", "actions[0].verb");
    ASSERT(t->actions[0].kind == AX25SDL_KIND_SIGNAL_UPPER, "actions[0].kind");
    return 0;
}

static int test_t08_info_not_permitted_in_frame(void) {
    const TransitionSpec* t = NULL;
    for (size_t i = 0; i < data_link_awaiting_v22_connection.transitions_len; i++) {
        if (strcmp(data_link_awaiting_v22_connection.transitions[i].id, "t08_info_not_permitted_in_frame") == 0) {
            t = &data_link_awaiting_v22_connection.transitions[i];
            break;
        }
    }
    ASSERT(t != NULL, "t08_info_not_permitted_in_frame not found");
    ASSERT_STREQ(t->on, "info_not_permitted_in_frame", "on");
    ASSERT_STREQ(t->next, "AwaitingV22Connection", "next");
    ASSERT(t->actions_len == 1, "actions count");
    ASSERT_STREQ(t->actions[0].verb, "DL-ERROR Indication (M)", "actions[0].verb");
    ASSERT(t->actions[0].kind == AX25SDL_KIND_SIGNAL_UPPER, "actions[0].kind");
    return 0;
}

static int test_t09_u_or_s_frame_length_error(void) {
    const TransitionSpec* t = NULL;
    for (size_t i = 0; i < data_link_awaiting_v22_connection.transitions_len; i++) {
        if (strcmp(data_link_awaiting_v22_connection.transitions[i].id, "t09_u_or_s_frame_length_error") == 0) {
            t = &data_link_awaiting_v22_connection.transitions[i];
            break;
        }
    }
    ASSERT(t != NULL, "t09_u_or_s_frame_length_error not found");
    ASSERT_STREQ(t->on, "u_or_s_frame_length_error", "on");
    ASSERT_STREQ(t->next, "AwaitingV22Connection", "next");
    ASSERT(t->actions_len == 1, "actions count");
    ASSERT_STREQ(t->actions[0].verb, "DL-ERROR Indication (N)", "actions[0].verb");
    ASSERT(t->actions[0].kind == AX25SDL_KIND_SIGNAL_UPPER, "actions[0].kind");
    return 0;
}

static int test_t10_ui_received_no(void) {
    const TransitionSpec* t = NULL;
    for (size_t i = 0; i < data_link_awaiting_v22_connection.transitions_len; i++) {
        if (strcmp(data_link_awaiting_v22_connection.transitions[i].id, "t10_ui_received_no") == 0) {
            t = &data_link_awaiting_v22_connection.transitions[i];
            break;
        }
    }
    ASSERT(t != NULL, "t10_ui_received_no not found");
    ASSERT_STREQ(t->on, "UI_received", "on");
    ASSERT_STREQ(t->next, "AwaitingV22Connection", "next");
    ASSERT_STREQ(t->guard, "not P_eq_1", "guard");
    ASSERT(t->actions_len == 2, "actions count");
    ASSERT_STREQ(t->actions[0].verb, "UI Check", "actions[0].verb");
    ASSERT(t->actions[0].kind == AX25SDL_KIND_SUBROUTINE, "actions[0].kind");
    ASSERT_STREQ(t->actions[1].verb, "DM (F = 1)", "actions[1].verb");
    ASSERT(t->actions[1].kind == AX25SDL_KIND_SIGNAL_LOWER, "actions[1].kind");
    return 0;
}

static int test_t10_ui_received_yes(void) {
    const TransitionSpec* t = NULL;
    for (size_t i = 0; i < data_link_awaiting_v22_connection.transitions_len; i++) {
        if (strcmp(data_link_awaiting_v22_connection.transitions[i].id, "t10_ui_received_yes") == 0) {
            t = &data_link_awaiting_v22_connection.transitions[i];
            break;
        }
    }
    ASSERT(t != NULL, "t10_ui_received_yes not found");
    ASSERT_STREQ(t->on, "UI_received", "on");
    ASSERT_STREQ(t->next, "AwaitingV22Connection", "next");
    ASSERT_STREQ(t->guard, "P_eq_1", "guard");
    ASSERT(t->actions_len == 1, "actions count");
    ASSERT_STREQ(t->actions[0].verb, "UI Check", "actions[0].verb");
    ASSERT(t->actions[0].kind == AX25SDL_KIND_SUBROUTINE, "actions[0].kind");
    return 0;
}

static int test_t11_dm_received_no(void) {
    const TransitionSpec* t = NULL;
    for (size_t i = 0; i < data_link_awaiting_v22_connection.transitions_len; i++) {
        if (strcmp(data_link_awaiting_v22_connection.transitions[i].id, "t11_dm_received_no") == 0) {
            t = &data_link_awaiting_v22_connection.transitions[i];
            break;
        }
    }
    ASSERT(t != NULL, "t11_dm_received_no not found");
    ASSERT_STREQ(t->on, "DM_received", "on");
    ASSERT_STREQ(t->next, "AwaitingConnection", "next");
    ASSERT_STREQ(t->guard, "not F_eq_1", "guard");
    ASSERT(t->actions_len == 0, "actions count");
    return 0;
}

static int test_t11_dm_received_yes(void) {
    const TransitionSpec* t = NULL;
    for (size_t i = 0; i < data_link_awaiting_v22_connection.transitions_len; i++) {
        if (strcmp(data_link_awaiting_v22_connection.transitions[i].id, "t11_dm_received_yes") == 0) {
            t = &data_link_awaiting_v22_connection.transitions[i];
            break;
        }
    }
    ASSERT(t != NULL, "t11_dm_received_yes not found");
    ASSERT_STREQ(t->on, "DM_received", "on");
    ASSERT_STREQ(t->next, "Disconnected", "next");
    ASSERT_STREQ(t->guard, "F_eq_1", "guard");
    ASSERT(t->actions_len == 3, "actions count");
    ASSERT_STREQ(t->actions[0].verb, "discard_I_frame_queue", "actions[0].verb");
    ASSERT(t->actions[0].kind == AX25SDL_KIND_PROCESSING, "actions[0].kind");
    ASSERT_STREQ(t->actions[1].verb, "DL-DISCONNECT Indication", "actions[1].verb");
    ASSERT(t->actions[1].kind == AX25SDL_KIND_SIGNAL_UPPER, "actions[1].kind");
    ASSERT_STREQ(t->actions[2].verb, "Stop T1", "actions[2].verb");
    ASSERT(t->actions[2].kind == AX25SDL_KIND_PROCESSING, "actions[2].kind");
    return 0;
}

static int test_t12_ua_received_no(void) {
    const TransitionSpec* t = NULL;
    for (size_t i = 0; i < data_link_awaiting_v22_connection.transitions_len; i++) {
        if (strcmp(data_link_awaiting_v22_connection.transitions[i].id, "t12_ua_received_no") == 0) {
            t = &data_link_awaiting_v22_connection.transitions[i];
            break;
        }
    }
    ASSERT(t != NULL, "t12_ua_received_no not found");
    ASSERT_STREQ(t->on, "UA_received", "on");
    ASSERT_STREQ(t->next, "AwaitingV22Connection", "next");
    ASSERT_STREQ(t->guard, "not F_eq_1", "guard");
    ASSERT(t->actions_len == 1, "actions count");
    ASSERT_STREQ(t->actions[0].verb, "DL-ERROR Indication (D)", "actions[0].verb");
    ASSERT(t->actions[0].kind == AX25SDL_KIND_SIGNAL_UPPER, "actions[0].kind");
    return 0;
}

static int test_t12_ua_received_yes_yes(void) {
    const TransitionSpec* t = NULL;
    for (size_t i = 0; i < data_link_awaiting_v22_connection.transitions_len; i++) {
        if (strcmp(data_link_awaiting_v22_connection.transitions[i].id, "t12_ua_received_yes_yes") == 0) {
            t = &data_link_awaiting_v22_connection.transitions[i];
            break;
        }
    }
    ASSERT(t != NULL, "t12_ua_received_yes_yes not found");
    ASSERT_STREQ(t->on, "UA_received", "on");
    ASSERT_STREQ(t->next, "Connected", "next");
    ASSERT_STREQ(t->guard, "F_eq_1 and layer_3_initiated", "guard");
    ASSERT(t->actions_len == 8, "actions count");
    ASSERT_STREQ(t->actions[0].verb, "DL-CONNECT Confirm", "actions[0].verb");
    ASSERT(t->actions[0].kind == AX25SDL_KIND_SIGNAL_UPPER, "actions[0].kind");
    ASSERT_STREQ(t->actions[1].verb, "Stop T1", "actions[1].verb");
    ASSERT(t->actions[1].kind == AX25SDL_KIND_PROCESSING, "actions[1].kind");
    ASSERT_STREQ(t->actions[2].verb, "Start T3", "actions[2].verb");
    ASSERT(t->actions[2].kind == AX25SDL_KIND_PROCESSING, "actions[2].kind");
    ASSERT_STREQ(t->actions[3].verb, "V(s) := 0", "actions[3].verb");
    ASSERT(t->actions[3].kind == AX25SDL_KIND_PROCESSING, "actions[3].kind");
    ASSERT_STREQ(t->actions[4].verb, "V(a) := 0", "actions[4].verb");
    ASSERT(t->actions[4].kind == AX25SDL_KIND_PROCESSING, "actions[4].kind");
    ASSERT_STREQ(t->actions[5].verb, "V(r) := 0", "actions[5].verb");
    ASSERT(t->actions[5].kind == AX25SDL_KIND_PROCESSING, "actions[5].kind");
    ASSERT_STREQ(t->actions[6].verb, "Select_T1_Value", "actions[6].verb");
    ASSERT(t->actions[6].kind == AX25SDL_KIND_SUBROUTINE, "actions[6].kind");
    ASSERT_STREQ(t->actions[7].verb, "MDL-NEGOTIATE Request", "actions[7].verb");
    ASSERT(t->actions[7].kind == AX25SDL_KIND_INTERNAL_OUT, "actions[7].kind");
    return 0;
}

static int test_t12_ua_received_yes_no_no(void) {
    const TransitionSpec* t = NULL;
    for (size_t i = 0; i < data_link_awaiting_v22_connection.transitions_len; i++) {
        if (strcmp(data_link_awaiting_v22_connection.transitions[i].id, "t12_ua_received_yes_no_no") == 0) {
            t = &data_link_awaiting_v22_connection.transitions[i];
            break;
        }
    }
    ASSERT(t != NULL, "t12_ua_received_yes_no_no not found");
    ASSERT_STREQ(t->on, "UA_received", "on");
    ASSERT_STREQ(t->next, "Connected", "next");
    ASSERT_STREQ(t->guard, "F_eq_1 and not layer_3_initiated and not vs_eq_va", "guard");
    ASSERT(t->actions_len == 11, "actions count");
    ASSERT_STREQ(t->actions[0].verb, "SRT := Initial Default", "actions[0].verb");
    ASSERT(t->actions[0].kind == AX25SDL_KIND_PROCESSING, "actions[0].kind");
    ASSERT_STREQ(t->actions[1].verb, "T1V := 2 * SRT", "actions[1].verb");
    ASSERT(t->actions[1].kind == AX25SDL_KIND_PROCESSING, "actions[1].kind");
    ASSERT_STREQ(t->actions[2].verb, "Start T3", "actions[2].verb");
    ASSERT(t->actions[2].kind == AX25SDL_KIND_PROCESSING, "actions[2].kind");
    ASSERT_STREQ(t->actions[3].verb, "DL-CONNECT Confirm", "actions[3].verb");
    ASSERT(t->actions[3].kind == AX25SDL_KIND_SIGNAL_UPPER, "actions[3].kind");
    ASSERT_STREQ(t->actions[4].verb, "Stop T1", "actions[4].verb");
    ASSERT(t->actions[4].kind == AX25SDL_KIND_PROCESSING, "actions[4].kind");
    ASSERT_STREQ(t->actions[5].verb, "Start T3", "actions[5].verb");
    ASSERT(t->actions[5].kind == AX25SDL_KIND_PROCESSING, "actions[5].kind");
    ASSERT_STREQ(t->actions[6].verb, "V(s) := 0", "actions[6].verb");
    ASSERT(t->actions[6].kind == AX25SDL_KIND_PROCESSING, "actions[6].kind");
    ASSERT_STREQ(t->actions[7].verb, "V(a) := 0", "actions[7].verb");
    ASSERT(t->actions[7].kind == AX25SDL_KIND_PROCESSING, "actions[7].kind");
    ASSERT_STREQ(t->actions[8].verb, "V(r) := 0", "actions[8].verb");
    ASSERT(t->actions[8].kind == AX25SDL_KIND_PROCESSING, "actions[8].kind");
    ASSERT_STREQ(t->actions[9].verb, "Select_T1_Value", "actions[9].verb");
    ASSERT(t->actions[9].kind == AX25SDL_KIND_SUBROUTINE, "actions[9].kind");
    ASSERT_STREQ(t->actions[10].verb, "MDL-NEGOTIATE Request", "actions[10].verb");
    ASSERT(t->actions[10].kind == AX25SDL_KIND_INTERNAL_OUT, "actions[10].kind");
    return 0;
}

static int test_t12_ua_received_yes_no_yes(void) {
    const TransitionSpec* t = NULL;
    for (size_t i = 0; i < data_link_awaiting_v22_connection.transitions_len; i++) {
        if (strcmp(data_link_awaiting_v22_connection.transitions[i].id, "t12_ua_received_yes_no_yes") == 0) {
            t = &data_link_awaiting_v22_connection.transitions[i];
            break;
        }
    }
    ASSERT(t != NULL, "t12_ua_received_yes_no_yes not found");
    ASSERT_STREQ(t->on, "UA_received", "on");
    ASSERT_STREQ(t->next, "Connected", "next");
    ASSERT_STREQ(t->guard, "F_eq_1 and not layer_3_initiated and vs_eq_va", "guard");
    ASSERT(t->actions_len == 7, "actions count");
    ASSERT_STREQ(t->actions[0].verb, "Stop T1", "actions[0].verb");
    ASSERT(t->actions[0].kind == AX25SDL_KIND_PROCESSING, "actions[0].kind");
    ASSERT_STREQ(t->actions[1].verb, "Start T3", "actions[1].verb");
    ASSERT(t->actions[1].kind == AX25SDL_KIND_PROCESSING, "actions[1].kind");
    ASSERT_STREQ(t->actions[2].verb, "V(s) := 0", "actions[2].verb");
    ASSERT(t->actions[2].kind == AX25SDL_KIND_PROCESSING, "actions[2].kind");
    ASSERT_STREQ(t->actions[3].verb, "V(a) := 0", "actions[3].verb");
    ASSERT(t->actions[3].kind == AX25SDL_KIND_PROCESSING, "actions[3].kind");
    ASSERT_STREQ(t->actions[4].verb, "V(r) := 0", "actions[4].verb");
    ASSERT(t->actions[4].kind == AX25SDL_KIND_PROCESSING, "actions[4].kind");
    ASSERT_STREQ(t->actions[5].verb, "Select_T1_Value", "actions[5].verb");
    ASSERT(t->actions[5].kind == AX25SDL_KIND_SUBROUTINE, "actions[5].kind");
    ASSERT_STREQ(t->actions[6].verb, "MDL-NEGOTIATE Request", "actions[6].verb");
    ASSERT(t->actions[6].kind == AX25SDL_KIND_INTERNAL_OUT, "actions[6].kind");
    return 0;
}

static int test_t13_t1_expiry_yes(void) {
    const TransitionSpec* t = NULL;
    for (size_t i = 0; i < data_link_awaiting_v22_connection.transitions_len; i++) {
        if (strcmp(data_link_awaiting_v22_connection.transitions[i].id, "t13_t1_expiry_yes") == 0) {
            t = &data_link_awaiting_v22_connection.transitions[i];
            break;
        }
    }
    ASSERT(t != NULL, "t13_t1_expiry_yes not found");
    ASSERT_STREQ(t->on, "T1_expiry", "on");
    ASSERT_STREQ(t->next, "Disconnected", "next");
    ASSERT_STREQ(t->guard, "RC_eq_N2", "guard");
    ASSERT(t->actions_len == 3, "actions count");
    ASSERT_STREQ(t->actions[0].verb, "discard_frame_queue", "actions[0].verb");
    ASSERT(t->actions[0].kind == AX25SDL_KIND_PROCESSING, "actions[0].kind");
    ASSERT_STREQ(t->actions[1].verb, "DL-ERROR Indication (G)", "actions[1].verb");
    ASSERT(t->actions[1].kind == AX25SDL_KIND_SIGNAL_UPPER, "actions[1].kind");
    ASSERT_STREQ(t->actions[2].verb, "DL-DISCONNECT Indication", "actions[2].verb");
    ASSERT(t->actions[2].kind == AX25SDL_KIND_SIGNAL_UPPER, "actions[2].kind");
    return 0;
}

static int test_t13_t1_expiry_no(void) {
    const TransitionSpec* t = NULL;
    for (size_t i = 0; i < data_link_awaiting_v22_connection.transitions_len; i++) {
        if (strcmp(data_link_awaiting_v22_connection.transitions[i].id, "t13_t1_expiry_no") == 0) {
            t = &data_link_awaiting_v22_connection.transitions[i];
            break;
        }
    }
    ASSERT(t != NULL, "t13_t1_expiry_no not found");
    ASSERT_STREQ(t->on, "T1_expiry", "on");
    ASSERT_STREQ(t->next, "AwaitingV22Connection", "next");
    ASSERT_STREQ(t->guard, "not RC_eq_N2", "guard");
    ASSERT(t->actions_len == 4, "actions count");
    ASSERT_STREQ(t->actions[0].verb, "RC := RC + 1", "actions[0].verb");
    ASSERT(t->actions[0].kind == AX25SDL_KIND_PROCESSING, "actions[0].kind");
    ASSERT_STREQ(t->actions[1].verb, "SABME (P = 1)", "actions[1].verb");
    ASSERT(t->actions[1].kind == AX25SDL_KIND_SIGNAL_LOWER, "actions[1].kind");
    ASSERT_STREQ(t->actions[2].verb, "Select_T1_Value", "actions[2].verb");
    ASSERT(t->actions[2].kind == AX25SDL_KIND_SUBROUTINE, "actions[2].kind");
    ASSERT_STREQ(t->actions[3].verb, "Start T1", "actions[3].verb");
    ASSERT(t->actions[3].kind == AX25SDL_KIND_PROCESSING, "actions[3].kind");
    return 0;
}

static int test_t14_frmr_received(void) {
    const TransitionSpec* t = NULL;
    for (size_t i = 0; i < data_link_awaiting_v22_connection.transitions_len; i++) {
        if (strcmp(data_link_awaiting_v22_connection.transitions[i].id, "t14_frmr_received") == 0) {
            t = &data_link_awaiting_v22_connection.transitions[i];
            break;
        }
    }
    ASSERT(t != NULL, "t14_frmr_received not found");
    ASSERT_STREQ(t->on, "FRMR_received", "on");
    ASSERT_STREQ(t->next, "AwaitingConnection", "next");
    ASSERT(t->actions_len == 5, "actions count");
    ASSERT_STREQ(t->actions[0].verb, "SRT := Initial Default", "actions[0].verb");
    ASSERT(t->actions[0].kind == AX25SDL_KIND_PROCESSING, "actions[0].kind");
    ASSERT_STREQ(t->actions[1].verb, "T1V := 2 * SRT", "actions[1].verb");
    ASSERT(t->actions[1].kind == AX25SDL_KIND_PROCESSING, "actions[1].kind");
    ASSERT_STREQ(t->actions[2].verb, "Establish_Data_Link", "actions[2].verb");
    ASSERT(t->actions[2].kind == AX25SDL_KIND_SUBROUTINE, "actions[2].kind");
    ASSERT_STREQ(t->actions[3].verb, "Set Layer 3 Initiated", "actions[3].verb");
    ASSERT(t->actions[3].kind == AX25SDL_KIND_PROCESSING, "actions[3].kind");
    ASSERT_STREQ(t->actions[4].verb, "set_version_2_0", "actions[4].verb");
    ASSERT(t->actions[4].kind == AX25SDL_KIND_PROCESSING, "actions[4].kind");
    return 0;
}

static int test_t15_sabme_received(void) {
    const TransitionSpec* t = NULL;
    for (size_t i = 0; i < data_link_awaiting_v22_connection.transitions_len; i++) {
        if (strcmp(data_link_awaiting_v22_connection.transitions[i].id, "t15_sabme_received") == 0) {
            t = &data_link_awaiting_v22_connection.transitions[i];
            break;
        }
    }
    ASSERT(t != NULL, "t15_sabme_received not found");
    ASSERT_STREQ(t->on, "SABME_received", "on");
    ASSERT_STREQ(t->next, "AwaitingV22Connection", "next");
    ASSERT(t->actions_len == 2, "actions count");
    ASSERT_STREQ(t->actions[0].verb, "F := P", "actions[0].verb");
    ASSERT(t->actions[0].kind == AX25SDL_KIND_PROCESSING, "actions[0].kind");
    ASSERT_STREQ(t->actions[1].verb, "UA", "actions[1].verb");
    ASSERT(t->actions[1].kind == AX25SDL_KIND_SIGNAL_LOWER, "actions[1].kind");
    return 0;
}

static int test_t16_sabm_received(void) {
    const TransitionSpec* t = NULL;
    for (size_t i = 0; i < data_link_awaiting_v22_connection.transitions_len; i++) {
        if (strcmp(data_link_awaiting_v22_connection.transitions[i].id, "t16_sabm_received") == 0) {
            t = &data_link_awaiting_v22_connection.transitions[i];
            break;
        }
    }
    ASSERT(t != NULL, "t16_sabm_received not found");
    ASSERT_STREQ(t->on, "SABM_received", "on");
    ASSERT_STREQ(t->next, "AwaitingConnection", "next");
    ASSERT(t->actions_len == 3, "actions count");
    ASSERT_STREQ(t->actions[0].verb, "F := P", "actions[0].verb");
    ASSERT(t->actions[0].kind == AX25SDL_KIND_PROCESSING, "actions[0].kind");
    ASSERT_STREQ(t->actions[1].verb, "UA", "actions[1].verb");
    ASSERT(t->actions[1].kind == AX25SDL_KIND_SIGNAL_LOWER, "actions[1].kind");
    ASSERT_STREQ(t->actions[2].verb, "set_version_2_0", "actions[2].verb");
    ASSERT(t->actions[2].kind == AX25SDL_KIND_PROCESSING, "actions[2].kind");
    return 0;
}

static int test_t17_disc_received(void) {
    const TransitionSpec* t = NULL;
    for (size_t i = 0; i < data_link_awaiting_v22_connection.transitions_len; i++) {
        if (strcmp(data_link_awaiting_v22_connection.transitions[i].id, "t17_disc_received") == 0) {
            t = &data_link_awaiting_v22_connection.transitions[i];
            break;
        }
    }
    ASSERT(t != NULL, "t17_disc_received not found");
    ASSERT_STREQ(t->on, "DISC_received", "on");
    ASSERT_STREQ(t->next, "AwaitingV22Connection", "next");
    ASSERT(t->actions_len == 2, "actions count");
    ASSERT_STREQ(t->actions[0].verb, "F := P", "actions[0].verb");
    ASSERT(t->actions[0].kind == AX25SDL_KIND_PROCESSING, "actions[0].kind");
    ASSERT_STREQ(t->actions[1].verb, "DM", "actions[1].verb");
    ASSERT(t->actions[1].kind == AX25SDL_KIND_SIGNAL_LOWER, "actions[1].kind");
    return 0;
}

static int test_t18_all_other_primitives__from_lower_layer(void) {
    const TransitionSpec* t = NULL;
    for (size_t i = 0; i < data_link_awaiting_v22_connection.transitions_len; i++) {
        if (strcmp(data_link_awaiting_v22_connection.transitions[i].id, "t18_all_other_primitives__from_lower_layer") == 0) {
            t = &data_link_awaiting_v22_connection.transitions[i];
            break;
        }
    }
    ASSERT(t != NULL, "t18_all_other_primitives__from_lower_layer not found");
    ASSERT_STREQ(t->on, "all_other_primitives__from_lower_layer", "on");
    ASSERT_STREQ(t->next, "AwaitingV22Connection", "next");
    ASSERT(t->actions_len == 0, "actions count");
    return 0;
}

int main(void) {
    int rc = 0;
    rc |= test_source_figure();
    rc |= test_transitions_count();
    rc |= test_t02_dl_connect_request();
    rc |= test_t03_dl_unit_data_request();
    rc |= test_t04_dl_data_request_yes();
    rc |= test_t04_dl_data_request_no();
    rc |= test_t05_i_frame_pops_off_queue_yes();
    rc |= test_t05_i_frame_pops_off_queue_no();
    rc |= test_t06_all_other_primitives__from_upper_layer();
    rc |= test_t07_control_field_error();
    rc |= test_t08_info_not_permitted_in_frame();
    rc |= test_t09_u_or_s_frame_length_error();
    rc |= test_t10_ui_received_no();
    rc |= test_t10_ui_received_yes();
    rc |= test_t11_dm_received_no();
    rc |= test_t11_dm_received_yes();
    rc |= test_t12_ua_received_no();
    rc |= test_t12_ua_received_yes_yes();
    rc |= test_t12_ua_received_yes_no_no();
    rc |= test_t12_ua_received_yes_no_yes();
    rc |= test_t13_t1_expiry_yes();
    rc |= test_t13_t1_expiry_no();
    rc |= test_t14_frmr_received();
    rc |= test_t15_sabme_received();
    rc |= test_t16_sabm_received();
    rc |= test_t17_disc_received();
    rc |= test_t18_all_other_primitives__from_lower_layer();
    return rc;
}
