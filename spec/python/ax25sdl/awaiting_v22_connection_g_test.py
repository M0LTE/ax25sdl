# Code generated by codegen/src/Packet.Sdl.CodeGen from spec-sdl/v2.2-errata/data-link/yaml/awaiting_v22_connection.sdl.yaml.
# DO NOT EDIT. Run `dotnet run --project codegen/src/Packet.Sdl.CodeGen` to regenerate.

from . import DATA_LINK_AWAITING_V_22_CONNECTION
from .types import ActionKind


def test_source_figure() -> None:
    assert DATA_LINK_AWAITING_V_22_CONNECTION.source.figure == "figc4.6"


def test_transitions_are_present() -> None:
    assert len(DATA_LINK_AWAITING_V_22_CONNECTION.transitions) == 25


def test_t02_dl_connect_request() -> None:
    t = next(
        (x for x in DATA_LINK_AWAITING_V_22_CONNECTION.transitions if x.id == "t02_dl_connect_request"),
        None,
    )
    assert t is not None, "transition t02_dl_connect_request not found"
    assert t.on == "DL_CONNECT_request"
    assert t.next == "AwaitingV22Connection"
    assert len(t.actions) == 2
    assert t.actions[0].verb == "Discard Queue"
    assert t.actions[0].kind == ActionKind.PROCESSING
    assert t.actions[1].verb == "Set Layer 3 Initiated"
    assert t.actions[1].kind == ActionKind.PROCESSING


def test_t03_dl_unit_data_request() -> None:
    t = next(
        (x for x in DATA_LINK_AWAITING_V_22_CONNECTION.transitions if x.id == "t03_dl_unit_data_request"),
        None,
    )
    assert t is not None, "transition t03_dl_unit_data_request not found"
    assert t.on == "DL_UNIT_DATA_request"
    assert t.next == "AwaitingV22Connection"
    assert len(t.actions) == 1
    assert t.actions[0].verb == "UI Command"
    assert t.actions[0].kind == ActionKind.SIGNAL_LOWER


def test_t04_dl_data_request_yes() -> None:
    t = next(
        (x for x in DATA_LINK_AWAITING_V_22_CONNECTION.transitions if x.id == "t04_dl_data_request_yes"),
        None,
    )
    assert t is not None, "transition t04_dl_data_request_yes not found"
    assert t.on == "DL_DATA_request"
    assert t.next == "AwaitingV22Connection"
    assert t.guard == "layer_3_initiated"
    assert len(t.actions) == 0


def test_t04_dl_data_request_no() -> None:
    t = next(
        (x for x in DATA_LINK_AWAITING_V_22_CONNECTION.transitions if x.id == "t04_dl_data_request_no"),
        None,
    )
    assert t is not None, "transition t04_dl_data_request_no not found"
    assert t.on == "DL_DATA_request"
    assert t.next == "AwaitingV22Connection"
    assert t.guard == "not layer_3_initiated"
    assert len(t.actions) == 1
    assert t.actions[0].verb == "push_frame_on_queue"
    assert t.actions[0].kind == ActionKind.INTERNAL_OUT


def test_t05_i_frame_pops_off_queue_yes() -> None:
    t = next(
        (x for x in DATA_LINK_AWAITING_V_22_CONNECTION.transitions if x.id == "t05_i_frame_pops_off_queue_yes"),
        None,
    )
    assert t is not None, "transition t05_i_frame_pops_off_queue_yes not found"
    assert t.on == "I_frame_pops_off_queue"
    assert t.next == "AwaitingV22Connection"
    assert t.guard == "layer_3_initiated"
    assert len(t.actions) == 0


def test_t05_i_frame_pops_off_queue_no() -> None:
    t = next(
        (x for x in DATA_LINK_AWAITING_V_22_CONNECTION.transitions if x.id == "t05_i_frame_pops_off_queue_no"),
        None,
    )
    assert t is not None, "transition t05_i_frame_pops_off_queue_no not found"
    assert t.on == "I_frame_pops_off_queue"
    assert t.next == "AwaitingV22Connection"
    assert t.guard == "not layer_3_initiated"
    assert len(t.actions) == 1
    assert t.actions[0].verb == "push_frame_on_queue"
    assert t.actions[0].kind == ActionKind.INTERNAL_OUT


def test_t06_all_other_primitives__from_lower_layer() -> None:
    t = next(
        (x for x in DATA_LINK_AWAITING_V_22_CONNECTION.transitions if x.id == "t06_all_other_primitives__from_lower_layer"),
        None,
    )
    assert t is not None, "transition t06_all_other_primitives__from_lower_layer not found"
    assert t.on == "all_other_primitives__from_lower_layer"
    assert t.next == "AwaitingV22Connection"
    assert len(t.actions) == 0


def test_t07_control_field_error() -> None:
    t = next(
        (x for x in DATA_LINK_AWAITING_V_22_CONNECTION.transitions if x.id == "t07_control_field_error"),
        None,
    )
    assert t is not None, "transition t07_control_field_error not found"
    assert t.on == "control_field_error"
    assert t.next == "AwaitingV22Connection"
    assert len(t.actions) == 1
    assert t.actions[0].verb == "DL-ERROR Indication (L)"
    assert t.actions[0].kind == ActionKind.SIGNAL_UPPER


def test_t08_info_field_permitted_in_frame() -> None:
    t = next(
        (x for x in DATA_LINK_AWAITING_V_22_CONNECTION.transitions if x.id == "t08_info_field_permitted_in_frame"),
        None,
    )
    assert t is not None, "transition t08_info_field_permitted_in_frame not found"
    assert t.on == "info_field_permitted_in_frame"
    assert t.next == "AwaitingV22Connection"
    assert len(t.actions) == 1
    assert t.actions[0].verb == "DL-ERROR Indication (M)"
    assert t.actions[0].kind == ActionKind.SIGNAL_UPPER


def test_t09_u_or_s_frame_length_error() -> None:
    t = next(
        (x for x in DATA_LINK_AWAITING_V_22_CONNECTION.transitions if x.id == "t09_u_or_s_frame_length_error"),
        None,
    )
    assert t is not None, "transition t09_u_or_s_frame_length_error not found"
    assert t.on == "u_or_s_frame_length_error"
    assert t.next == "AwaitingV22Connection"
    assert len(t.actions) == 1
    assert t.actions[0].verb == "DL-ERROR Indication (N)"
    assert t.actions[0].kind == ActionKind.SIGNAL_UPPER


def test_t10_ui_received_no() -> None:
    t = next(
        (x for x in DATA_LINK_AWAITING_V_22_CONNECTION.transitions if x.id == "t10_ui_received_no"),
        None,
    )
    assert t is not None, "transition t10_ui_received_no not found"
    assert t.on == "UI_received"
    assert t.next == "AwaitingV22Connection"
    assert t.guard == "not P_eq_1"
    assert len(t.actions) == 2
    assert t.actions[0].verb == "UI Check"
    assert t.actions[0].kind == ActionKind.SUBROUTINE
    assert t.actions[1].verb == "DM (F = 1)"
    assert t.actions[1].kind == ActionKind.SIGNAL_LOWER


def test_t10_ui_received_yes() -> None:
    t = next(
        (x for x in DATA_LINK_AWAITING_V_22_CONNECTION.transitions if x.id == "t10_ui_received_yes"),
        None,
    )
    assert t is not None, "transition t10_ui_received_yes not found"
    assert t.on == "UI_received"
    assert t.next == "AwaitingV22Connection"
    assert t.guard == "P_eq_1"
    assert len(t.actions) == 1
    assert t.actions[0].verb == "UI Check"
    assert t.actions[0].kind == ActionKind.SUBROUTINE


def test_t11_dm_received_no() -> None:
    t = next(
        (x for x in DATA_LINK_AWAITING_V_22_CONNECTION.transitions if x.id == "t11_dm_received_no"),
        None,
    )
    assert t is not None, "transition t11_dm_received_no not found"
    assert t.on == "DM_received"
    assert t.next == "AwaitingConnection"
    assert t.guard == "not F_eq_1"
    assert len(t.actions) == 0


def test_t11_dm_received_yes() -> None:
    t = next(
        (x for x in DATA_LINK_AWAITING_V_22_CONNECTION.transitions if x.id == "t11_dm_received_yes"),
        None,
    )
    assert t is not None, "transition t11_dm_received_yes not found"
    assert t.on == "DM_received"
    assert t.next == "Disconnected"
    assert t.guard == "F_eq_1"
    assert len(t.actions) == 3
    assert t.actions[0].verb == "discard_I_frame_queue"
    assert t.actions[0].kind == ActionKind.PROCESSING
    assert t.actions[1].verb == "DL-DISCONNECT Indication"
    assert t.actions[1].kind == ActionKind.SIGNAL_UPPER
    assert t.actions[2].verb == "Stop T1"
    assert t.actions[2].kind == ActionKind.PROCESSING


def test_t12_ua_received_no() -> None:
    t = next(
        (x for x in DATA_LINK_AWAITING_V_22_CONNECTION.transitions if x.id == "t12_ua_received_no"),
        None,
    )
    assert t is not None, "transition t12_ua_received_no not found"
    assert t.on == "UA_received"
    assert t.next == "AwaitingV22Connection"
    assert t.guard == "not F_eq_1"
    assert len(t.actions) == 1
    assert t.actions[0].verb == "DL-ERROR Indication (D)"
    assert t.actions[0].kind == ActionKind.SIGNAL_UPPER


def test_t12_ua_received_yes_yes() -> None:
    t = next(
        (x for x in DATA_LINK_AWAITING_V_22_CONNECTION.transitions if x.id == "t12_ua_received_yes_yes"),
        None,
    )
    assert t is not None, "transition t12_ua_received_yes_yes not found"
    assert t.on == "UA_received"
    assert t.next == "Connected"
    assert t.guard == "F_eq_1 and layer_3_initiated"
    assert len(t.actions) == 8
    assert t.actions[0].verb == "DL-CONNECT Confirm"
    assert t.actions[0].kind == ActionKind.SIGNAL_UPPER
    assert t.actions[1].verb == "Stop T1"
    assert t.actions[1].kind == ActionKind.PROCESSING
    assert t.actions[2].verb == "Start T3"
    assert t.actions[2].kind == ActionKind.PROCESSING
    assert t.actions[3].verb == "V(s) := 0"
    assert t.actions[3].kind == ActionKind.PROCESSING
    assert t.actions[4].verb == "V(a) := 0"
    assert t.actions[4].kind == ActionKind.PROCESSING
    assert t.actions[5].verb == "V(r) := 0"
    assert t.actions[5].kind == ActionKind.PROCESSING
    assert t.actions[6].verb == "Select_T1_Value"
    assert t.actions[6].kind == ActionKind.SUBROUTINE
    assert t.actions[7].verb == "MDL-NEGOTIATE Request"
    assert t.actions[7].kind == ActionKind.INTERNAL_OUT


def test_t12_ua_received_yes_no_no() -> None:
    t = next(
        (x for x in DATA_LINK_AWAITING_V_22_CONNECTION.transitions if x.id == "t12_ua_received_yes_no_no"),
        None,
    )
    assert t is not None, "transition t12_ua_received_yes_no_no not found"
    assert t.on == "UA_received"
    assert t.next == "Connected"
    assert t.guard == "F_eq_1 and not layer_3_initiated and not vs_eq_va"
    assert len(t.actions) == 11
    assert t.actions[0].verb == "SRT := Initial Default"
    assert t.actions[0].kind == ActionKind.PROCESSING
    assert t.actions[1].verb == "T1V := 2 * SRT"
    assert t.actions[1].kind == ActionKind.PROCESSING
    assert t.actions[2].verb == "Start T3"
    assert t.actions[2].kind == ActionKind.PROCESSING
    assert t.actions[3].verb == "DL-CONNECT Confirm"
    assert t.actions[3].kind == ActionKind.SIGNAL_UPPER
    assert t.actions[4].verb == "Stop T1"
    assert t.actions[4].kind == ActionKind.PROCESSING
    assert t.actions[5].verb == "Start T3"
    assert t.actions[5].kind == ActionKind.PROCESSING
    assert t.actions[6].verb == "V(s) := 0"
    assert t.actions[6].kind == ActionKind.PROCESSING
    assert t.actions[7].verb == "V(a) := 0"
    assert t.actions[7].kind == ActionKind.PROCESSING
    assert t.actions[8].verb == "V(r) := 0"
    assert t.actions[8].kind == ActionKind.PROCESSING
    assert t.actions[9].verb == "Select_T1_Value"
    assert t.actions[9].kind == ActionKind.SUBROUTINE
    assert t.actions[10].verb == "MDL-NEGOTIATE Request"
    assert t.actions[10].kind == ActionKind.INTERNAL_OUT


def test_t12_ua_received_yes_no_yes() -> None:
    t = next(
        (x for x in DATA_LINK_AWAITING_V_22_CONNECTION.transitions if x.id == "t12_ua_received_yes_no_yes"),
        None,
    )
    assert t is not None, "transition t12_ua_received_yes_no_yes not found"
    assert t.on == "UA_received"
    assert t.next == "Connected"
    assert t.guard == "F_eq_1 and not layer_3_initiated and vs_eq_va"
    assert len(t.actions) == 7
    assert t.actions[0].verb == "Stop T1"
    assert t.actions[0].kind == ActionKind.PROCESSING
    assert t.actions[1].verb == "Start T3"
    assert t.actions[1].kind == ActionKind.PROCESSING
    assert t.actions[2].verb == "V(s) := 0"
    assert t.actions[2].kind == ActionKind.PROCESSING
    assert t.actions[3].verb == "V(a) := 0"
    assert t.actions[3].kind == ActionKind.PROCESSING
    assert t.actions[4].verb == "V(r) := 0"
    assert t.actions[4].kind == ActionKind.PROCESSING
    assert t.actions[5].verb == "Select_T1_Value"
    assert t.actions[5].kind == ActionKind.SUBROUTINE
    assert t.actions[6].verb == "MDL-NEGOTIATE Request"
    assert t.actions[6].kind == ActionKind.INTERNAL_OUT


def test_t13_t1_expiry_yes() -> None:
    t = next(
        (x for x in DATA_LINK_AWAITING_V_22_CONNECTION.transitions if x.id == "t13_t1_expiry_yes"),
        None,
    )
    assert t is not None, "transition t13_t1_expiry_yes not found"
    assert t.on == "T1_expiry"
    assert t.next == "Disconnected"
    assert t.guard == "RC_eq_N2"
    assert len(t.actions) == 3
    assert t.actions[0].verb == "discard_frame_queue"
    assert t.actions[0].kind == ActionKind.PROCESSING
    assert t.actions[1].verb == "DL-ERROR Indication (G)"
    assert t.actions[1].kind == ActionKind.SIGNAL_UPPER
    assert t.actions[2].verb == "DL-DISCONNECT Indication"
    assert t.actions[2].kind == ActionKind.SIGNAL_UPPER


def test_t13_t1_expiry_no() -> None:
    t = next(
        (x for x in DATA_LINK_AWAITING_V_22_CONNECTION.transitions if x.id == "t13_t1_expiry_no"),
        None,
    )
    assert t is not None, "transition t13_t1_expiry_no not found"
    assert t.on == "T1_expiry"
    assert t.next == "AwaitingV22Connection"
    assert t.guard == "not RC_eq_N2"
    assert len(t.actions) == 4
    assert t.actions[0].verb == "RC := RC + 1"
    assert t.actions[0].kind == ActionKind.PROCESSING
    assert t.actions[1].verb == "SABME (P = 1)"
    assert t.actions[1].kind == ActionKind.SIGNAL_LOWER
    assert t.actions[2].verb == "Select_T1_Value"
    assert t.actions[2].kind == ActionKind.SUBROUTINE
    assert t.actions[3].verb == "Start T1"
    assert t.actions[3].kind == ActionKind.PROCESSING


def test_t14_frmr_received() -> None:
    t = next(
        (x for x in DATA_LINK_AWAITING_V_22_CONNECTION.transitions if x.id == "t14_frmr_received"),
        None,
    )
    assert t is not None, "transition t14_frmr_received not found"
    assert t.on == "FRMR_received"
    assert t.next == "AwaitingConnection"
    assert len(t.actions) == 5
    assert t.actions[0].verb == "SRT := Initial Default"
    assert t.actions[0].kind == ActionKind.PROCESSING
    assert t.actions[1].verb == "T1V := 2 * SRT"
    assert t.actions[1].kind == ActionKind.PROCESSING
    assert t.actions[2].verb == "Establish_Data_Link"
    assert t.actions[2].kind == ActionKind.SUBROUTINE
    assert t.actions[3].verb == "Set Layer 3 Initiated"
    assert t.actions[3].kind == ActionKind.PROCESSING
    assert t.actions[4].verb == "set_version_2_0"
    assert t.actions[4].kind == ActionKind.PROCESSING


def test_t15_sabme_received() -> None:
    t = next(
        (x for x in DATA_LINK_AWAITING_V_22_CONNECTION.transitions if x.id == "t15_sabme_received"),
        None,
    )
    assert t is not None, "transition t15_sabme_received not found"
    assert t.on == "SABME_received"
    assert t.next == "AwaitingV22Connection"
    assert len(t.actions) == 2
    assert t.actions[0].verb == "F := P"
    assert t.actions[0].kind == ActionKind.PROCESSING
    assert t.actions[1].verb == "UA"
    assert t.actions[1].kind == ActionKind.SIGNAL_LOWER


def test_t16_sabm_received() -> None:
    t = next(
        (x for x in DATA_LINK_AWAITING_V_22_CONNECTION.transitions if x.id == "t16_sabm_received"),
        None,
    )
    assert t is not None, "transition t16_sabm_received not found"
    assert t.on == "SABM_received"
    assert t.next == "AwaitingConnection"
    assert len(t.actions) == 3
    assert t.actions[0].verb == "F := P"
    assert t.actions[0].kind == ActionKind.PROCESSING
    assert t.actions[1].verb == "UA"
    assert t.actions[1].kind == ActionKind.SIGNAL_LOWER
    assert t.actions[2].verb == "set_version_2_0"
    assert t.actions[2].kind == ActionKind.PROCESSING


def test_t17_disc_received() -> None:
    t = next(
        (x for x in DATA_LINK_AWAITING_V_22_CONNECTION.transitions if x.id == "t17_disc_received"),
        None,
    )
    assert t is not None, "transition t17_disc_received not found"
    assert t.on == "DISC_received"
    assert t.next == "AwaitingV22Connection"
    assert len(t.actions) == 2
    assert t.actions[0].verb == "F := P"
    assert t.actions[0].kind == ActionKind.PROCESSING
    assert t.actions[1].verb == "DM"
    assert t.actions[1].kind == ActionKind.SIGNAL_LOWER


def test_t18_all_other_primitives__from_upper_layer() -> None:
    t = next(
        (x for x in DATA_LINK_AWAITING_V_22_CONNECTION.transitions if x.id == "t18_all_other_primitives__from_upper_layer"),
        None,
    )
    assert t is not None, "transition t18_all_other_primitives__from_upper_layer not found"
    assert t.on == "all_other_primitives__from_upper_layer"
    assert t.next == "AwaitingV22Connection"
    assert len(t.actions) == 0

